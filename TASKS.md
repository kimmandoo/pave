# Tasks

## Active implementation plan: Pave coding agent

The full planned feature set is still in progress. Tests must establish behavior per subsystem before a capability is marked complete. Preserve the mobile-specific system prompt, manifest discovery and per-command shell approval.

### Phase 1 — Provider and conversation contracts

- [x] Implemented the OCaml CLI, bounded tool loop, session recovery and initial mobile workspace tools.
- [x] Implemented OpenAI-compatible Chat Completions and Anthropic Messages, buffered and SSE-streaming, with local HTTP/tool-call verification.
- [x] Added thirty-four provider descriptors and fourteen wire route variants. OAuth/device sign-in is wired for Anthropic/Codex/OpenRouter/personal Copilot; Azure resource Responses, Sakana/Abliteration Responses, GMI Cloud/Moonshot Chat, Ollama Cloud native Chat, Google Vertex ADC Gemini SSE, AWS SigV4 buffered Converse, Bedrock Mantle bearer Responses, xAI reasoning-preserving Chat and NVIDIA hosted Chat each passed isolated authenticated function-call/result fixtures. Bedrock signed foundation, Mantle bearer, xAI and NVIDIA listings use dynamic account/provider responses; no model ID/default is bundled.
- [ ] Complete the model catalog, remaining provider transports and provider-specific thinking, usage, errors, multimodal content, proprietary gateway state and account entitlements; Gemini 3 signed tool calls replay correctly, and Ollama, OpenAI Responses, Codex subscription Responses, cache-aware Anthropic Messages, thoughts-inclusive Google Gemini and usage-reporting Chat Completions routes record actual token counts, but other routes and semantics still require parity work.
- [ ] Implement every remaining authentication policy and its matching inference transport. The generic authorization-code and device-code engines exist, but only Anthropic, Codex, OpenRouter and personal GitHub Copilot sign-in are wired to CLI inference. Copilot currently supports only the pinned public Chat endpoint; its Chat model IDs come from account discovery, not a built-in allowlist. Enterprise, Responses and Anthropic routes remain missing. A new provider should change one descriptor plus a transport only if its wire protocol differs.
- [ ] Provide the complete sibling `../oh-my-pi` provider inventory with matching usable authentication, native wire routes, account-scoped model discovery and tool-result turns. The sibling rules declare 83 providers; Pave has 34 genuinely exercised inference descriptors and 49 remaining, not name-only stubs. Unsupported families include many compatible gateway overlays, mixed Responses/Anthropic routes and provider-native cloud/OAuth transports. Validate each endpoint, capabilities and login isolation; never copy sibling default model IDs or infer universal compatibility from Chat syntax.

### Phase 2 — Session and interactive runtime (in progress)

- [x] Replaced flat session JSON with an append-only parent-linked JSONL journal, migrated existing transcripts, added branch/fork commands and resumed interrupted tool calls with explicit failure results.
- [x] Added manual model-generated compaction with a durable summary boundary; preserved full journal history and recovered incomplete tool results.
- [x] Added in-session `/login` with real browser callback and private-store grant, and `/model` provider/model selection with preserved conversation; the terminal temporarily leaves alternate-screen mode for browser URLs and restores the transcript afterward. Fresh interactive starts without existing defaults now offer a versioned, user-scoped SETUP provider/auth/model flow and `/setup` re-entry; explicit CLI/headless sessions bypass it. API keys are never entered in the TUI.
- [ ] Implement automatic context budgeting, provider-native compaction, session metadata and full resume semantics beyond the current message journal. `/new` creates private per-workspace journals; `/resume` searches/reopens them with history and branch-local model selection restored (unless explicit CLI flags override it). Tool/mode metadata, labels/pins and token budgeting remain missing.
- [ ] Implement cancellable event-driven turns, queued steering and tool-call lifecycle events, then finish the terminal's component model, transcript, composer, overlays and accessibility behavior. Typed worker-to-UI model/tool phase notices now identify a currently executing tool in the header and restore model progress for follow-up requests; full lifecycle events and accessibility work remain. Verify real interactive UX with keyboard and resize flows.

### Phase 3 — Tools and IDE integrations

- [ ] Complete the built-in read/edit/grep/glob/bash contracts, approvals, job ownership and specialized tools without claiming that the existing seven simplified tools are equivalent.
- [ ] Implement LSP server lifecycle, navigation, diagnostics and write-through refactors; implement DAP launch/attach, breakpoints, stepping and inspection.

### Phase 4 — Delegation, extensibility and auxiliary packages

- [ ] Implement subagents/worktrees/structured yields and async task/job lifecycle.
- [ ] Implement persistent JS/Python eval kernels, tool bridge, browser/CDP, MCP, skills and extension hooks.
- [ ] Implement the catalog, native services, TUI widgets, statistics, collaboration, snapshot/compaction and other useful capabilities enumerated in the reference-derived backlog below.

### Phase 5 — Maintainability pass (final review after product capabilities)

- [x] Grouped the current provider, auth, session, agent, tool and terminal modules and their tests into responsibility-based directories without changing public OCaml module names; separated CLI credential commands/selection from runtime orchestration.
- [ ] Revisit the **final** dependency graph after new capabilities land; keep public contracts narrow, split newly oversized files along actual responsibilities, remove superseded code paths and update callers, tests, build rules and contributor docs together. Verify CLI, PTY and provider behavior after the final move.

### Product completion requirements

- [ ] Profile and bound idle CPU, memory growth, subprocess lifetime and redraws during long sessions; keep keyboard interactions responsive without polling loops or unnecessary allocations.
- [x] Removed remaining legacy brand mentions from published files and local commit history while retaining required MIT copyright and permission notices in `THIRD_PARTY_NOTICES`; remote cache copies cannot be erased by Git history rewriting.

## Reference-derived implementation backlog

Source paths below are relative to the inspected sibling reference tree. Tasks describe useful behavior to build in native OCaml, not instructions to copy its Bun, npm, Bazel, Rust/N-API, hosted service or branding. Each item remains open until its user-facing scenario and security boundary are verified. The earlier checked items are Pave's **current baseline**, not full equivalence.

### P0 — Updating, configuration and project context

- [x] Ship `pave update` for installer-owned native macOS/Linux binaries: compile the same auditable `install.sh` into the binary, verify release SHA-256/archive structure, preserve the configured installation directory, ignore stale version/destination environment overrides, reject unmanaged/source builds and keep the executable usable after a bad download. Exercise a custom directory and corrupted archive. Reference: `packages/coding-agent/src/{commands/update.ts,cli/update-cli.ts}`, `scripts/install.sh`.
- [x] Added `pave update --check` for installer-owned tagged binaries: compared the embedded version to bounded HTTPS latest-release metadata without writing, reported available/current/newer pinned states and failed closed on unavailable metadata. Source/opam/foreign-managed binaries still refuse self-update. Exercised all comparison branches and the no-write invariant in an isolated installed fixture. Reference: `packages/coding-agent/src/cli/update-cli.ts`.
- [x] Added typed user/project JSON defaults with CLI > project > user precedence, invalid-file diagnostics, a shell-disable policy that cannot grant execution, and a searchable `/settings` editor that atomically writes private project settings for the next launch. Verified inheritance, symlink rejection, PTY edits and `--allow-shell` denial. Reference: `packages/coding-agent/src/config/{settings.ts,settings-schema.ts}`, `docs/settings.md`.
- [x] Loaded user/ancestor `AGENTS.md` with explicit precedence, bounded `@` imports, conflict/shadow diagnostics and `.pave/rules` path matching. A model-requested file edit/write is withheld until the matching safe rule has been delivered as system context; unsafe targets/imports fail closed. Exercised scoped tool turns, cancellation and journal pairing in local fixtures. Reference: `docs/{context-files,rulebook-matching-pipeline}.md`, `packages/coding-agent/src/{discovery,capability/rule.ts}`.
- [x] Added bounded `SYSTEM.md`/`SYSTEM_TEMPLATE.md`/`APPEND_SYSTEM.md` discovery with project > user and explicit CLI override precedence, strict `{{root}}` templates, diagnostics and preserved mobile safety/ancestor instructions; no tool output is promoted to system authority. Exercised actual Responses CLI instructions and malformed/symlink fixtures. Reference: `docs/system-prompt-customization.md`, `packages/coding-agent/src/system-prompt.ts`.
- [ ] Evaluate platform additions independently: Windows and musl builds, per-architecture packaged-binary smoke, update verification and native-binding availability; never advertise an unbuilt platform. Release jobs now unpack and execute their matching macOS/Linux arm64/x86-64 archives before publication. macOS signing/notarization is **optional distribution hardening**, not a prerequisite for this terminal executable or a blocker for other tasks; Windows, musl and native bindings remain open. Reference: `docs/{macos-signing-notarization,natives-build-release-debugging}.md`, `packages/coding-agent/src/cli/update-cli.ts`.

### P1 — Model catalog, providers and authenticated inference

- [ ] Build a provider-derived model catalog with account-scoped IDs, canonical aliases only when verified, provider/API identity, reported context/output limits, pricing, modalities, tool support and thinking/effort controls; never bake model IDs or implicit model-family wire routing into the executable. Keep verified routes distinct from guessed compatibility. Reference: `packages/catalog/src/{models.ts,model-manager.ts,model-thinking.ts}`, `docs/models.md`.
- [ ] Add authenticated provider model discovery and safe cache/freshness rules across all routes. `/model`, `/settings` and first-run setup use cancellable pinned OpenAI, Gemini, Ollama, personal Copilot, Codex OAuth, OpenRouter user-filtered, API-key Anthropic (bounded pages), DeepSeek, Groq, Mistral, Together (chat only), Cerebras, Venice (tool-capable text), DeepInfra, Fireworks (bounded serverless cursor), Baseten, Hugging Face, NanoGPT, AIML API (advertised Chat tools), ai& (tool-capable), Sakana, Abliteration, GMI Cloud, AWS-signed Bedrock on-demand text foundations and local LM Studio/llama.cpp/vLLM listings. Bedrock foundation IDs are not proof of account Invoke permission or tool support; GMI/Sakana listings omit per-model tool flags. Azure deployments, Google Vertex, Bedrock inference profiles, Anthropic OAuth and other providers have no discovery; durable account-scoped cache and freshness policy remain missing. No bundled model ID list or implicit default.
- [ ] Permit explicitly configured providers/models with per-route wire API, endpoint, auth/key precedence and declared capabilities; never forward OAuth tokens to caller-chosen hosts. Reference: `packages/catalog/src/compat/resolve.ts`, `docs/providers.md`.
- [ ] Expand normalized message/stream contracts to image, signed thinking, audio/video when available, typed tool output, refusal/incomplete stop and provider-reported token/cache usage before claiming multimodal parity. Reference: `packages/ai/src/{types.ts,providers/transform-messages.ts}`, `docs/provider-streaming-internals.md`.
- [ ] Complete existing Chat, Responses, Codex, Anthropic, Gemini and Ollama wire families: provider-specific schema/choice/reasoning, native continuation, prompt caching, streaming usage, incomplete responses, errors and recovery without executing partial calls. Chat streaming now requires an explicit valid finish reason and refuses tool calls labeled `stop`; other partial/variant provider semantics still need work. Reference: `packages/ai/src/providers/{openai-completions.ts,openai-responses.ts,openai-codex-responses.ts,anthropic.ts,google-shared.ts,ollama.ts}`.
- [ ] Add provider-specific compatible-gateway overlays rather than globally reusing Chat: OpenRouter, Azure deployments, Vercel AI Gateway, Cloudflare AI Gateway, Kimi, Synthetic, Alibaba plans, DeepSeek, Mistral IDs, Fireworks/Firepass, ClinePass, xAI, Z.AI and Qwen thinking/headers/pricing where their actual routes require them. Reference: `packages/catalog/src/compat/rules/providers/`, `docs/provider-endpoint-constraints.md`.
- [ ] Complete Google Vertex and Google Cloud Code Assist/Gemini CLI/Antigravity as separate identity and wire routes. Vertex now uses derived validated project/location Google hosts, explicit bearer or gcloud ADC/metadata tokens and preserves signed native Gemini tool-result replay across SSE turns; a native CLI fake HTTPS scenario exercised a real workspace result. Vertex account model discovery, direct ADC JSON without gcloud, OAuth Code Assist/Gemini CLI/Antigravity and Claude wire remain missing. Reference: `packages/ai/src/providers/{google-vertex.ts,google-gemini-cli.ts,google-auth.ts}`, `packages/catalog/src/provider-models/google.ts`.
- [ ] Complete Azure OpenAI: Responses with explicit validated public-cloud resource/deployment and API-key auth now passes a native real-workspace tool-result turn in an isolated fake HTTPS fixture. Deployment discovery, Azure Chat, Entra ID, other clouds and provider-specific semantics remain missing; do not confuse a model ID with a deployment. Reference: `packages/ai/src/providers/azure-openai-responses.ts`, `packages/catalog/src/compat/rules/providers/azure.kdl`.
- [ ] Complete AWS Bedrock Converse/SigV4 binary EventStream, full credential chain, inference-profile discovery and region permissions; expand distinct Bedrock Mantle bearer transport where account-visible IDs do not establish Responses compatibility or tool support. Buffered native Converse tool/result turns sign SHA-256/HMAC requests using static AWS env/shared profiles; signed `ListFoundationModels` filters active on-demand text entries, without implying Invoke permission. Native CLI fake HTTPS exercised two signed regional requests around real `read_file` output. Mantle now sends fixed regional bearer Responses, replays tool results and filters unavailable account IDs from bounded `/v1/models`, with isolated two-turn fixture but no live entitlement. SSO/role/web identity/ECS/IMDS, binary ConverseStream, other Mantle wire routes and exact per-model capabilities remain unimplemented. Never treat Bedrock or Mantle as plain OpenAI Chat. Reference: `packages/ai/src/providers/{amazon-bedrock.ts,aws-credentials.ts,aws-sigv4.ts,aws-eventstream.ts,bedrock-mantle.ts}`.
- [ ] Implement Cursor, Devin, GitLab Duo API and GitLab workflow as independently authenticated/discovered proprietary transports with real tool/result turn fixtures. Reference: `packages/ai/src/providers/{cursor.ts,devin.ts,gitlab-duo.ts,gitlab-duo-workflow.ts}`, `packages/catalog/src/discovery/`.
- [ ] Complete personal Copilot entitled Chat/Responses/Anthropic routes; add GitHub Enterprise only with validated domain, client and credential isolation. Public Chat model IDs are now fetched from the authenticated account's pinned endpoint rather than restricted to a baked-in two-model list; non-Chat API transports and live entitlement validation remain open. Reference: `packages/catalog/src/wire/github-copilot.ts`, `packages/ai/src/providers/github-copilot-headers.ts`.
- [ ] Expand local inference beyond Ollama, LM Studio, llama.cpp and vLLM Chat where actually served: Ollama Cloud/model tags and Apple Foundation Models on supported Apple hosts; handle missing binaries/models explicitly. The three local OpenAI-compatible hosts use private/loopback validated base URLs, optional API keys, independent keyless Chat wire and bounded model listing; their model and tool support depends on the installed local server/template. Reference: `packages/catalog/src/provider-models/{ollama.ts,openai-compat.ts}`, `packages/ai/src/providers/apple-foundation-models.ts`, `docs/local-models.md`.
- [ ] Add remaining verified API-key-backed model providers from the catalog in route-compatible batches (Cerebras, Baseten, DeepInfra, Together, CoreWeave, NVIDIA, Hugging Face, Moonshot, StepFun, Meta, NanoGPT, Novita, Venice, MiniMax, Qianfan, SiliconFlow, Kilo and others); require the actual provider's endpoint, model policy, authentication and a tool-turn fixture, not a name-only descriptor. Reference: `packages/catalog/src/provider-models/descriptors.ts`, `docs/providers.md`.
- [ ] Implement non-chat model kinds only behind real consumers: embeddings, reranking, images, speech/transcription and video; do not count these as chat routes. Reference: `packages/catalog/src/types.ts`, `packages/ai/src/providers/{embeddings-server.ts,images-server.ts,rerank-server.ts,speech-server.ts,transcriptions-server.ts,video-server.ts}`.
- [ ] Classify provider status/auth/context/rate-limit failures, first-event and idle timeouts, tool fallback and retry boundaries; retry only before visible output or side effects, record real input/output/cache/premium usage and pricing provenance. Reference: `packages/ai/src/providers/register-builtins.ts`, `packages/catalog/src/pricing.ts`, `docs/provider-endpoint-constraints.md`.

### P2 — Sign-in, accounts and secret safety

- [ ] Add the remaining authorization-code flows with **matching inference**: separate Google Gemini CLI/Antigravity clients and project hooks, GitLab Duo and Agent, Devin, Stencil and Z.AI coding plan; preserve each provider's state/PKCE/redirect requirements rather than weakening shared OAuth. Reference: `packages/catalog/src/compat/rules/auth/`, `packages/ai/src/registry/oauth/`.
- [ ] Add device grants for Kimi Code, xAI OAuth, Muse Code and headless Codex; handle provider-specific device identity, polling, refresh and service-token exchange. Reference: `packages/ai/src/registry/oauth/{kimi.ts,xai-oauth.ts,muse-code.ts,openai-codex.ts}`, `packages/catalog/src/compat/rules/auth/`.
- [ ] Implement nonstandard account/key provisioning only with usable inference: Cursor, Perplexity, Alibaba Coding/Token Plan regional credentials, Xiaomi, Cloudflare account/gateway/key, Kilo and Copilot Enterprise. Do not mislabel API-key prompts as OAuth. Reference: `packages/ai/src/registry/oauth/`, `packages/catalog/src/compat/rules/auth/_order.kdl`.
- [ ] Add multiple accounts per provider, identity/entitlement display and selection, private refresh/rotation under locks, revoked-account handling and provider-scoped key precedence without silently mixing workspaces. Reference: `packages/ai/src/auth/`, `packages/ai/src/registry/oauth/index.ts`, `docs/providers.md`.
- [ ] Offer opt-in provider-bound secret masking in prompts/tool results with safe reversible placeholders for tool arguments, one-way redaction for export/logging and documented limits; never assume regex alone prevents all leaks. Reference: `packages/coding-agent/src/secrets/`, `docs/secrets.md`.
- [ ] Evaluate remote auth broker/credential vault only if a real deployable service and trust model exist; keep local token storage and refresh safe in the meantime. Reference: `docs/auth-broker-gateway.md`.

### P3 — Agent turns, sessions and context

- [ ] Give every model/tool turn a cancellable owner and typed start/delta/end/error events; safely pair unfinished calls with results and keep the UI responsive without polling or lost drafts. Reference: `packages/agent/src/{agent.ts,agent-loop.ts,types.ts}`, `packages/coding-agent/src/session/agent-session.ts`.
- [ ] Add steering and queued follow-ups with explicit submission boundaries, interrupt/retry/dequeue and exactly-once side effects; preserve the editor draft across cancellation. Reference: `packages/coding-agent/src/session/queued-messages.ts`, `packages/coding-agent/src/modes/controllers/input-controller.ts`.
- [ ] Define a typed tool scheduler (argument validation, dynamic tool availability, shared/exclusive execution, progress, cancellation and structured text/image results) with ordered model-facing call/results. Reference: `packages/agent/src/agent-loop.ts`, `packages/coding-agent/src/tools/{index.ts,tool-result.ts}`.
- [ ] Add per-tool/per-argument read/write/exec approval modes with deny precedence, clear impact preview, headless behavior and compound-command safety; retain Pave's current per-command shell prompt as baseline. Reference: `packages/coding-agent/src/tools/approval.ts`, `docs/approval-mode.md`.
- [ ] Persist model/thinking/tool/mode changes, titles, labels, pins, attachments and session lineage as journal metadata distinct from provider messages; implement recent search, `/resume`, `/new`, `/clear`, `/fresh` and safe branch/fork navigation. `/new` and `/resume` manage private journals, `/tree` searches and checks out parent-linked branches, and provider/model selection is durable branch-local metadata; thinking/tool/mode metadata, labels/pins, attachments and remaining lifecycle commands still require design and verification. Reference: `packages/coding-agent/src/session/{session-manager.ts,session-listing.ts,session-entries.ts}`, `docs/session.md`.
- [ ] Record tool start/settle/abort and session exit so restart distinguishes unfinished from completed side effects without rerunning them; preserve provider-valid assistant/tool adjacency and branch isolation. Reference: `packages/coding-agent/src/session/{turn-persistence.ts,turn-recovery.ts}`, `docs/session.md`.
- [ ] Budget context by real model token/window constraints; auto/manual/provider-native compaction, branch summaries and bounded trimming must preserve immutable journal history, image artifacts and signed provider replay where valid. Reference: `packages/agent/src/compaction/`, `packages/coding-agent/src/session/compaction-methods.ts`, `docs/compaction.md`.
- [ ] Add durable artifacts for large outputs/images and session-owned async jobs with list/read/wait/cancel and exactly-once completion delivery; no cross-session job leakage. Reference: `packages/coding-agent/src/{async/job-manager.ts,session/artifacts.ts,session/async-job-delivery.ts}`, `docs/tools/wait.md`.
- [ ] Add bounded child-agent delegation, structured results, restricted tools and cancellation; optional isolated worktrees/merge only after parent/child ownership and approval work. Reference: `packages/coding-agent/src/task/`, `docs/tools/task.md`.
- [ ] Add opt-in plan/proposal, goals, loop, advisor/watchdog, autoresearch and rule-triggered interruption as distinct reviewed workflows after cancellation/approvals; make model usage and side effects visible. Reference: `packages/coding-agent/src/{plan-mode,goals,advisor,autoresearch}/`, `docs/{advisor-watchdog,ttsR-injection-lifecycle,vibe-mode}.md`.
- [ ] Support safe checkpoint/rewind of workspace effects alongside journal branches, explicitly identifying commands that cannot be undone. Reference: `packages/coding-agent/src/tools/{checkpoint.ts,resolve.ts}`, `docs/tools/rewind.md`.

### P4 — Workspace tools, IDE and network

- [ ] Implement the full workspace read/search contracts. Gitignore-aware `glob` now supports `*`, `?`, `**`, classes and nested negation; bounded Str-regex `grep` rejects unsafe repeated expressions and both exclude hidden names by default and avoid following symlinks. Native CLI tool-call/result and isolated malicious-pattern, negation and boundary regressions passed. Remaining: full selective readers for directory/URL/document/archive/notebook/SQLite/internal URIs and broader safe search semantics. Reference: `packages/coding-agent/src/tools/{read.ts,grep.ts,glob.ts,read-archive.ts,read-sqlite.ts,fetch.ts}`, `docs/notebook-tool-runtime.md`.
- [ ] Add snapshot/conflict-aware multi-hunk and AST edit/rename, deterministic previews and exact failure reporting; do not degrade the current unique-match atomic edit. Reference: `packages/coding-agent/src/{edit/,tools/ast-edit.ts,tools/write.ts}`.
- [ ] Add cancellable foreground shell, PTY where supported and explicit managed background/service jobs with cwd/env/timeouts, bounded output, readiness, stdin and kill; keep separate approval for each command. Reference: `packages/coding-agent/src/{exec/bash-executor.ts,async/job-manager.ts,tools/bash-interactive.ts}`, `docs/tools/bash.md`.
- [ ] Add managed LSP lifecycle, definitions/references/diagnostics/hover/rename/code actions with write-through sync and guarded edits; prefer actual language-server semantics over text search. Reference: `packages/coding-agent/src/lsp/`, `docs/tools/lsp.md`.
- [ ] Add DAP adapter launch/attach, breakpoints, thread/frame/variable inspection, stepping/evaluate and cleanup with read-versus-execute approval. Reference: `packages/coding-agent/src/dap/`, `docs/tools/debug.md`.
- [ ] Add persistent per-session JavaScript/Python evaluation kernels with explicit reset, timeout/cancel, package policy and safe bridge to tools/jobs; never claim kernel state survives a restart. Reference: `packages/coding-agent/src/eval/`, `docs/tools/eval.md`.
- [ ] Add web search with explicit provider credentials/priority, source URLs/citations, bounded content and error handling; add web fetch/HTML-to-Markdown as a separate network-trust boundary. Reference: `packages/coding-agent/src/web/search/`, `docs/tools/web_search.md`.
- [ ] Add optional SSH workspace reads/transfers/commands with host verification, remote path guards and the same approvals as local tools; do not infer permission from a local workspace root. Reference: `packages/coding-agent/src/ssh/`, `packages/coding-agent/src/internal-urls/ssh-protocol.ts`.
- [ ] Add useful native services individually (fast file search, PDF/OCR, git, clipboard, terminal image, token count, process supervision) with fallback/errors for unsupported hosts; do not wholesale import Rust/N-API build machinery. Reference: `packages/natives/README.md`, `docs/natives-media-system-utils.md`.
- [ ] Add opt-in repository security scanning and finding validation with read/write/exec authorization, provenance and SARIF export; never auto-publish secrets or treat unvalidated model findings as facts. Reference: `packages/coding-agent/src/security/`, `docs/tools/security_scan.md`.
- [ ] Add safe Git/worktree/commit assistance only with explicit branch/commit approval and an isolated workspace story; preserve user changes. Reference: `packages/coding-agent/src/{commit,task/worktree.ts}`, `docs/tools/task.md`.

### P5 — Terminal interaction and discoverability

- [ ] Move model execution off the input loop; display live model/tool state, allow cancellation/queued typing, preserve transcript/draft through errors, resize and auth handoff. Reference: `packages/coding-agent/src/modes/controllers/{event-controller.ts,input-controller.ts}`, `docs/tui-runtime-internals.md`.
- [ ] Implement wrapped grapheme-correct multiline editing, word/line operations, undo/kill/yank, history search, optional Vim and external editor; preserve CJK/emoji cursor position and drafts on narrow resizes. Bounded atomic undo/redo and kill/yank are now present; Vim/external editor and configurable bindings remain. Reference: `packages/tui/src/{components/editor.ts,vim.ts,keybindings.ts,overlays/history-search.ts}`.
- [ ] Make bracketed paste one bounded atomic edit with sanitized line endings/controls, no pasted Enter submission and visible handling of large text/images; test fragmented delimiters and CJK decomposed filenames. Reference: `packages/tui/src/{bracketed-paste.ts,stdin-buffer.ts,components/editor.ts}`.
- [ ] Replace the truncated plain-text transcript with semantic assistant/thinking/tool/Markdown/code/diff blocks, persistent scrollback, expandable results and an accurate streaming/settled boundary. Semantic roles, Markdown/code styles, settled tool previews and `Alt+O` expansion are present; separate thinking/diff blocks and full durable scrollback remain. Reference: `packages/tui/src/chrome/transcript-container.ts`, `packages/tui/src/chat/`, `docs/tui-core-renderer.md`.
- [ ] Harden terminal capabilities: width-correct CJK/emoji rendering, bounded dirty-row repaints and resize replay, suspend/restore, SSH/tmux/narrow/no-color fallback; opt-in mouse/graphics/clipboard only when supported. Reference: `packages/tui/src/{tui.ts,terminal-capabilities.ts,stdin-buffer.ts}`, `docs/tui-runtime-internals.md`.
- [ ] Add reusable keyboard-accessible overlays for provider/model/account/thinking selection, sessions/tree/search, tool approval, settings and errors; cancellation must not submit a draft. Reference: `packages/tui/src/{components/select-list.ts,overlays/}`, `docs/tui.md`.
- [ ] Show real provider/model, session, elapsed/tool progress, token/context/cost/limit and errors in a legible adaptive status line; never invent usage data for subscriptions. The TUI header now shows timed active model/tool progress or cached reported input/output totals for the idle selected branch or ephemeral conversation, and `/context` shows branch-local OpenAI Responses, Codex subscription Responses, Anthropic Messages, Google Gemini, Ollama and usage-reporting Chat Completions counts. Missing usage, context limits and prices remain unknown; full tool lifecycle progress still needs completion. Reference: `packages/tui/src/status-line/`, `packages/coding-agent/src/modes/status-line-host.ts`.
- [ ] Migrate existing slash handlers into one discoverable registry with aliases, argument validation, completion and truthful help; add core `/resume`, `/tree`, `/new`, `/retry`, `/tools`, `/context`, `/usage`, `/hotkeys`, `/settings` only with working backends. Existing commands have a typed catalog, live filtered `/` hints with actual descriptions, keyboard selection that inserts without executing, `/setup` re-entry and matching descriptive `/help`; `/tools [NAME]` lists actual shell-policy-filtered model tools, `/tree` checks out bounded journal ancestry, `/retry` safely reissues only a tool-free last turn, `/context` reports recorded conversation sizes and actual OpenAI Responses/Codex subscription Responses/Anthropic Messages/Google Gemini/Ollama/usage-reporting Chat Completions token counts, `/usage` groups recorded branch tokens by model, and `/hotkeys` shows actual terminal shortcuts. Other requests' usage and argument-aware completion remain open. Reference: `packages/coding-agent/src/slash-commands/`, `docs/slash-command-internals.md`.
- [ ] Expose configurable conflict-checked keybindings, focus precedence, Escape interruption versus dialog dismissal, prompt/path/model/skill completion and accessible fallback shortcuts. Reference: `packages/tui/src/{app-keybindings.ts,autocomplete.ts,hotkeys-markdown.ts}`.
- [ ] Make advanced slash groups conditional on implemented runtimes rather than no-op stubs: plan/goal/vibe, agent/jobs/todo, extensions/plugins/skills/MCP, browser/media/security/SSH, sharing/collaboration. Reference: `packages/coding-agent/src/slash-commands/builtin-{modes,session,lifecycle,control,collaboration,marketplace,skills}.ts`.
- [ ] Support headless stdin/file/image prompt inputs, text versus structured JSON event output and clean exit codes without rendering a terminal; retain the existing `--prompt` path. Reference: `packages/coding-agent/src/modes/print-mode.ts`, `docs/cli-reference.md`.
- [ ] Make turn-scoped instruction shortcuts opt-in, visibly indicated and restricted to user prose (never pasted code, filenames or tool output); allow each shortcut to be disabled. Reference: `packages/coding-agent/src/modes/magic-keywords.ts`, `docs/magic-keywords.md`.

### P6 — Extensibility, remote clients and auxiliary features

- [ ] Load bounded `SKILL.md` files from user/project and compatible locations with precedence, frontmatter/resources, conflict diagnostics and opt-in `/skill:<name>` activation; no implicit script execution. Reference: `packages/coding-agent/src/extensibility/skills.ts`, `docs/skills.md`.
- [ ] Register declarative custom prompt commands and approved custom tools with typed schemas and source attribution; add lifecycle hooks/extensions only with cancellation/disposal and a clear sandbox/trust policy. Reference: `packages/coding-agent/src/extensibility/`, `docs/{custom-tools,extensions,hooks}.md`.
- [ ] Manage versioned plugins and marketplaces (install/list/enable/disable/upgrade/doctor, user/project scope, provenance and explicit reload); expose a skill registry only with a real trusted service. Reference: `packages/coding-agent/src/extensibility/plugins/`, `packages/coding-agent/src/skillshare/`, `docs/marketplace.md`.
- [ ] Integrate MCP stdio and Streamable HTTP with secret-safe configuration, list/call/resources/prompts, timeouts/cancel/reload and partial-server diagnostics; add SSE and OAuth only after the core bridge. Reference: `packages/coding-agent/src/mcp/`, `docs/{mcp-config,mcp-runtime-lifecycle}.md`.
- [ ] Add isolated headless browser/CDP and a separately opt-in local Chrome-tab relay with URL/tab scope, bounded scripts, screenshot access and cleanup. Reference: `packages/coding-agent/src/tools/browser/`, `packages/browser-relay/README.md`, `docs/computer-use.md`.
- [ ] Expose local machine-readable events and stdio JSONL RPC (prompt/steer/abort/session/model/tool-approval), then an ACP adapter if a stable interop contract exists. Reference: `packages/coding-agent/src/modes/rpc/`, `docs/rpc.md`.
- [ ] Add optional collaboration after RPC: host/guest roles, host-only tool execution, encrypted invite/reconnect/backfill and a usable relay; browser guest UI requires a genuinely available service, not another project's hosted endpoint. Reference: `packages/coding-agent/src/collab/`, `packages/collab-web/README.md`, `docs/collab.md`.
- [ ] Add consented transcript export/share/recording with secret redaction and replay controls; do not broadcast session data as default telemetry. Reference: `packages/coding-agent/src/{export,stream/}`, `docs/session-operations-export-share-fork-resume.md`.
- [ ] Record per-turn usage and TTFT accurately before adding local-only stats/JSON/dashboard by model/project/day; Ollama, OpenAI Responses, Codex subscription Responses, cache-inclusive Anthropic Messages, Google Gemini and usage-reporting Chat Completions counts persist as private branch-local per-completion markers (including compaction), but requests without reported usage, TTFT and pricing remain open. Clearly label estimated subscription costs. Reference: `packages/stats/README.md`, `packages/coding-agent/src/cli/stats-cli.ts`.
- [ ] Add opt-in durable recall memory with project/global banks, search/edit/delete/export and explicit retention; defer embeddings/consolidation until consent and storage boundaries are clear. Reference: `packages/mnemopi/README.md`, `docs/mnemosyne-memory-backend.md`.
- [ ] Evaluate vision-backed snapshot compaction only after image-capable transports, persisted image artifacts and token budgets; compare against existing summary, never silently replace it. Reference: `packages/snapcompact/README.md`, `docs/compaction.md`.
- [ ] Add opt-in speech-to-text, text-to-speech, voice selection and local-model download state with platform permission/cancel handling; realtime audio/WebRTC comes after basic media. Reference: `packages/coding-agent/src/{stt,tts,live}/`, `docs/natives-media-system-utils.md`.
- [ ] Add self-hosted GitHub issue/PR triage automation only after safe RPC, worktrees, credential isolation and webhook verification; do not assume a pre-existing bot service. Reference: `python/robomp/README.md`.
- [ ] Add opt-in edit/tool/model evaluation and reproducible fixture benchmarks without making upstream container/VM tooling a user dependency. Reference: `packages/metaharness/README.md`, `packages/typescript-edit-benchmark/`.

Do not mark a phase complete merely because its module compiles. The full product plan has not been achieved.
