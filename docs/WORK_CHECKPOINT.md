# Work checkpoint

- **Date:** 2026-09-25
- **Active task:** The user prioritizes a beautiful, robust TUI, a modern model default and the complete reference-derived task inventory. `v0.1.7` is published across macOS/Linux arm64/x86-64. An isolated previously published `v0.1.6` binary upgraded over actual HTTPS to `v0.1.7`; `pave update --check` reported current and its PTY displayed the verified-model picker. OpenAI defaults to `gpt-6-sol` on Responses; personal Copilot stays guarded to its older supported Chat models. **Full reference parity is not complete.**
- **Current unreleased vertical slice:** Pure `bin/ui/transcript_view.ml` semantic roles, sanitized Markdown/code, bounded lazy grapheme wrapping, finished/interrupted tool states and `Alt+O` expandable results. `lib/ui/composer.ml` has bounded undo/redo, atomic paste and kill/yank with real lower-case Meta shortcuts. `lib/session/session_store.ml` creates private per-workspace journals; `/new`, `/resume` and `--session` restoration keep navigation and unsent drafts safe. `Tui` alone owns repaint and key dispatch; agent and provider contracts are unchanged.
- **Exact next action:** Review this integrated slice for release, run the complete strict test/install build after final edits, commit/tag/publish four-platform `v0.1.8` and verify an isolated real public update plus native PTY. Then continue unchecked TASKS.md work in end-to-end vertical slices, especially native session metadata, accurate provider usage/context, approval and real tool/IDE lifecycle; do not claim unavailable vendor entitlements or services.
- **Verification:** Full `opam exec -- dune runtest --force` and `opam exec -- dune build @install` passed after semantic/shortcut integration. Actual local-Ollama PTYs at 80×24 and resized 30×10 streamed role/code blocks, preserved Unicode drafts through undo/redo, created 0600 workspace journals, confirmed unsaved-discard prompt, and restored saved history from `/resume`. A 40×12 `NO_COLOR=1` PTY displayed a restored journal without non-reset colored SGR. A real agent `read_file` turn in the PTY showed compact output, expanded all file lines and re-collapsed on `Alt+O`, then exited cleanly. The shortcut changed from `Ctrl+O` after a PTY smoke revealed macOS terminal VDISCARD intercepted that byte.
- **Limits:** Remaining unchecked TASKS.md items include provider-specific semantics and model usage, automatic context budgeting, thinking/diff blocks, full session metadata/lineage, LSP/DAP, subagents, extensibility and remote services. No live vendor credentials, proprietary services, mobile-device builds, Windows/musl binaries or independent update signature were verified. Source/opam binaries cannot self-update; use their package manager.
