# Work checkpoint

- **Date:** 2026-09-25
- **Active task:** The user requested a substantially better TUI, a current default model and completion of the full reference-derived task inventory with maintainable boundaries. `v0.1.5` was published across four native targets and publicly installed/updated in an isolated directory; the ASCII launch logo was committed separately. This wave implemented GPT-6 Sol/Responses by default, searchable model/login/settings pickers, cancellable event-driven turns with queued prompts, real provider model listing for four providers, typed settings, bounded project instruction loading, upgraded file tools and read-only update checks. Two inventory items were verified and checked off; dozens remain open. **Full reference parity is not complete.**
- **Exact next action:** Commit and publish `v0.1.6` with four-platform CI, then use the already installed `v0.1.5` public binary to run `pave update` to the new public release and verify `pave update --check` against actual GitHub metadata. Continue unchecked TASKS.md items end to end: attach scoped project rules to per-file tool context and authenticated listings to a responsive model picker, then address remaining provider/session/tool/extensibility workflows individually with real fixtures and safety boundaries.
- **Changed files in this wave:** `bin/ui/{tui,terminal_input,settings_view}.ml`, `lib/ui/{composer,turn_runner}.ml`, `bin/main.ml`, `lib/agent/agent.ml`, `lib/tools/tools.ml`, `lib/config/{settings,project_context}.ml`, `lib/provider/{provider,provider_catalog,model_discovery}.ml`, `bin/update.ml` and release embedding/workflow now own the added runtime, UI, catalog and update contracts. Related `test/{agent,config,provider,tools,ui}`, build definitions, README, SECURITY, DESIGN_RULES, CONTRIBUTING and CHANGELOG were updated.
- **Verification:** `opam exec -- dune runtest --force` passed all current suites; `opam exec -- dune build @install` and tagged `PAVE_RELEASE_VERSION=v0.1.6 opam exec -- dune build --profile release bin/main.exe` passed. A local Responses HTTP server captured the actual CLI default `gpt-6-sol` and returned an answer. Native PTY smoke showed the ASCII logo, model/settings overlays, private settings writes and policy denial, a streamed cancelled turn with a queued follow-up and no residual provisional text, error-preserved unsent draft and subsequent valid SSE answer, plus 10,100 terminal rows bounded to the most recent 9,100 after eviction. Local Ollama tags served actual `--models` CLI output; a marker-owned tagged binary's `pave update --check` reported newer/current/older versions, failed closed on metadata outage and retained its SHA-256. All vendor inference calls remain fixture-only.
- **Limits:** This wave has not published `v0.1.6`; path-scoped rules are implemented in the loader but not yet attached to tool operations; live model discovery is CLI-only, with no Codex discovery or entitlement-aware picker; TUI scrollback is capped at 10,000 rows. Other TASKS.md items remain unchecked, including many proprietary transports/services requiring independent trust models and account entitlements. Native updater manifests share their release origin and are not independently signed. Source/opam binaries must update via their package manager; live vendor credentials and mobile device builds were not used.
