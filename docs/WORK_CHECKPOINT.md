# Work checkpoint

- **Date:** 2026-09-24
- **Active task:** The user prioritizes maintainable source ownership and reference-style in-session `/login` and `/model`, then full provider/auth parity. Library and test directories are grouped by responsibility. Ten provider descriptors and six wire payload formats have isolated fixture verification, including Gemini 3 signed tool turns and GitHub Copilot personal device-code/Chat. No live vendor account was exercised. The public MIT repository, installer, release artifacts, README and mark remain available.
- **Exact next action:** Continue remaining reference OAuth/provider transports and real model catalog/selector semantics; Copilot still lacks Enterprise, Responses, Anthropic and dynamic discovery. Then complete TUI/agent/tools/extensibility parity and final responsibility review. Keep model-bound opaque state and login security across route changes, and verify every new route through actual transport scenarios before advertising it.
- **Changed files in this wave:** `lib/provider/transports/gemini_wire.ml` and `gemini_stream.ml` retain and validate provider-signed native parts, while `lib/ui/interaction.ml` isolates Gemini/Codex state across model changes. `lib/auth/github_copilot_oauth.ml` defines public GitHub device login; `lib/provider/transports/github_copilot_wire.ml`, `lib/provider/provider_catalog.ml`, `lib/provider/provider.ml` and `bin/cli_auth.ml` enforce a pinned Copilot Chat route, token guards and device sign-in. Corresponding `test/auth/`, `test/provider/` and `test/ui/` fixtures and provider/security/contributor docs were updated.
- **Verification:** `opam exec -- dune runtest --force` passed 27 suites and `opam exec -- dune build @install` succeeded after the Copilot startup fix. Native Gemini 3 CLI exercised signed function calls, tool results and a reopened private journal over local HTTP, plus a separate SSE tool turn. Copilot CLI exercised fake HTTPS device authorization, 0700/0600 private store, foreign endpoint and unsupported-model rejection before network access, two authenticated Chat tool turns and logout. A native PTY exercised `/login github-copilot`, `/model github-copilot/gpt-4.1`, a tool turn and `/quit` while draining terminal redraws. The no-model startup failure was reproduced before editing, and a second native PTY verified deferred `/model` selection, unsupported-model rejection and `/quit` afterward. These were isolated fixtures, **not live provider account verification**.
- **Limits:** Live vendor credentials, OAuth app availability, model entitlements, actual mobile device builds and full provider catalog remain unverified. Most reference auth policies/transports and many agent/tools/TUI/extensibility features remain incomplete. Earlier mandatory MIT notices remain in `THIRD_PARTY_NOTICES`; rewriting local/remote Git refs does not erase copies held by outside caches.
