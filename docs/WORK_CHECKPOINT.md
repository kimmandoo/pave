# Work checkpoint

- **Date:** 2026-09-24
- **Active task:** Complete the reference provider/authentication surface before the remaining agent, terminal, tools and extensibility phases. Seven provider descriptors and five wire transports now have local-fixture verification. Anthropic OAuth browser/manual login, private storage, refresh and protected inference passed an isolated fixture, but no live account was exercised. The public MIT repository, one-command installer, release artifacts, README and pixel-art mark are in place. Perform the requested directory/file responsibility restructuring only after product capabilities are finished.
- **Exact next action:** Expand from Anthropic OAuth to the remaining registered authorization-code/device-code policies and matching inference transports (Codex requires account-scoped Responses; Google subscription, Copilot and other gateways require distinct protocols). Preserve unsupported-model fail-closed behavior and verify each provider with a real transport scenario before advertising it. Then finish TUI/agent/tools phases, and reorganize the directory graph last.
- **Changed files this provider wave:** `lib/provider_catalog.ml`, `lib/provider.ml`, `lib/agent.ml`, `lib/openai_responses_wire.ml`, `lib/openai_responses_stream.ml`, `lib/ollama_wire.ml`, `lib/ollama_stream.ml`, `lib/gemini_wire.ml`, `lib/gemini_stream.ml`, `lib/oauth_flow.ml`, `lib/oauth_device.ml`, `lib/oauth_store.ml`, `bin/main.ml`, `lib/dune`, `dune-project`, `test/dune`, matching protocol/auth tests, `THIRD_PARTY_NOTICES`, `README.md`, `TASKS.md`, `CHANGELOG.md`, `SECURITY.md`, `docs/DESIGN_RULES.md`, this checkpoint. Earlier TUI, installer, license and public-repository files were committed separately.
- **Verification:** `opam exec -- dune runtest --force` passed 20 suites and `opam exec -- dune build @install` succeeded. The native CLI handled seven provider descriptors against local HTTP fixtures; actual browser callback, token exchange, 0700/0600 store, expired-token refresh, Anthropic bearer completion and logout ran via an isolated fake HTTPS transport, and a foreign OAuth endpoint was rejected. Earlier opam source install, anonymous public HTTPS installer, real CJK PTY, local checksum corruption, lint and shell checks passed. Public CI passed four jobs and `v0.1.1` GitHub Release passed all four architecture builds and publishing.
- **Limits:** Live vendor credentials, supported-account entitlements, actual mobile device builds, Codex and most reference auth policies/transports have not been exercised or implemented. Many agent/tools/TUI/extensibility features remain incomplete. Earlier mandatory MIT notices remain in `THIRD_PARTY_NOTICES`; rewriting local/remote Git refs does not erase copies held by outside caches.
