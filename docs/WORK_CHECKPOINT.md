# Work checkpoint

- **Date:** 2026-09-24
- **Active task:** Complete the mobile coding agent's planned features and publish an easily installed, standalone open-source repository. The public README now foregrounds install, usage and contribution steps with a pixel-art logo. After product features are complete, split directories and files by responsibility with all callers, tests and build rules updated. Cancellation, provider catalog, LSP/DAP, subagents and many planned tools remain incomplete.
- **Exact next action:** Monitor the public CI matrix and `v0.1.0` four-platform release build; fix any failed runners or artifacts, then exercise the actual unauthenticated HTTPS installer and `pave --help`. Resume unfinished product capabilities in `TASKS.md`.
- **Changed files this session:** `bin/tui.ml`, `bin/terminal_input.ml`, `bin/main.ml`, `bin/dune`, `lib/composer.ml`, `lib/dune`, `test/test_composer.ml`, `test/dune`, `install.sh`, `THIRD_PARTY_NOTICES`, `.github/`, `assets/pave-mark.svg`, `.gitignore`, `dune-project`, `pave.opam`, `README.md`, `TASKS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, `docs/DESIGN_RULES.md`, `docs/TROUBLESHOOTING.md`, `CHANGELOG.md`, this checkpoint.
- **Verification:** `opam install . --deps-only -y && opam install . -y` installed the local source package; `opam exec -- pave --help` launched it. `opam exec -- dune runtest --force` passed 11 behavioral suites, `dune build --profile release bin/main.exe` built, `opam lint pave.opam`, `actionlint` for CI/release workflows, `sh -n install.sh` and `shellcheck install.sh` passed. A real PTY displayed fragmented Korean/Chinese/Japanese input, recovered from incomplete/repeated escapes, displayed replacements for malformed UTF-8 and exited cleanly. A local archive smoke installed the release binary and both notices, launched `pave --help` and refused a checksum-mismatched replacement without altering the installed executable. Earlier PTY/local HTTP smoke covered resize, multiline paste, streamed/buffered CJK and bounded long transcript.
- **Blockers:** Remote `main` and tag history now have one clean root commit; the repository is public and Actions are enabled, but public CI and all four release binaries have not yet passed. The first release must finish before anonymous installation can succeed. Real credentialed providers and device builds were not exercised. Retain all MIT copyright and permission notices from `LICENSE`; rewriting local/remote refs cannot guarantee erasure of externally cached copies.
