# Work checkpoint

- **Date:** 2026-09-24
- **Active task:** The latest user prioritizes maintainable folder/file ownership and interactive `/login` and `/model` parity with the reference. Library, executable UI and test modules are now grouped by responsibility; CLI authentication commands and credential selection have their own module. Nine provider descriptors and six wire transports have isolated fixture verification, with no live vendor account exercised. The public MIT repository, installer, release artifacts, README and mark remain available.
- **Exact next action:** Implement actual in-session browser login (without corrupting the full-screen terminal), plus interactive provider/model selection with history continuity and a way to enter the UI before credentials exist. Exercise both commands through native PTY and line CLI; then resume remaining provider transports, model semantics, TUI/agent/tools phases and final maintainability review.
- **Changed files in this wave:** `lib/` and `test/` modules moved to cohesive responsibility directories without renaming their OCaml modules; `lib/dune`, `test/dune`, `bin/dune` now include unqualified subdirectories. `bin/main.ml` delegates sign-in and credential selection to `bin/cli_auth.ml`; terminal files moved to `bin/ui/`. Updated `CONTRIBUTING.md`, `TASKS.md`, `CHANGELOG.md`, `docs/TROUBLESHOOTING.md` and this checkpoint.
- **Verification:** Following the structural cutover, `opam exec -- dune runtest --force` passed 24 suites and `opam exec -- dune build @install` succeeded. A native CLI fixture listed providers, exchanged a manual OpenRouter browser code for a private stored key, completed authenticated inference and removed the grant; a PTY initialized the full-screen UI, rendered `/help` and restored the terminal on `/quit`. Earlier Codex browser callback, refresh, SSE/tool turns, session reopening, encrypted-reasoning replay and enterprise residency passed isolated fake-HTTPS fixtures; no vendor calls were made. Earlier source install, anonymous public HTTPS installer, CJK PTY, lint and public four-platform `v0.1.2` publishing passed.
- **Limits:** Live vendor credentials, account entitlements and actual mobile device builds remain unverified. Most reference auth policies/transports and many agent/tools/TUI/extensibility features remain incomplete. Earlier mandatory MIT notices remain in `THIRD_PARTY_NOTICES`; rewriting local/remote Git refs does not erase copies held by outside caches.
