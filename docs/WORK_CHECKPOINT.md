# Work checkpoint

- **Date:** 2026-09-25
- **Active task:** The user prioritizes a beautiful, robust TUI, a modern model default and the full reference-derived task inventory. `v0.1.12` is published across macOS/Linux arm64/x86-64; release workflow `36051717710` passed. An explicitly isolated public `v0.1.11` installation upgraded over actual HTTPS to `v0.1.12` and `pave update --check` reported current. OpenAI defaults to `gpt-6-sol` on Responses; personal Copilot stays guarded to its older supported Chat models. **Full reference parity is not complete.**
- **Delivered TUI slice:** Pure semantic transcript, sanitized Markdown/code and tool lifecycle blocks, lazy grapheme wrapping, `Alt+O` expansion, bounded atomic undo/paste/kill/yank, private `/new`/`/resume`, and authoritative `/branch`/`/fork` transcript restoration. The public v0.1.9 PTY checked out a saved second turn then the first, proving abandoned rows disappeared after navigation.
- **Delivered TUI slice:** `Interaction.commands` is the single typed source for slash parsing, `/help` descriptions and `/` + Tab prefix-filtered chooser. `/tools [NAME]` uses the exact shell-policy-filtered provider schemas; `/tree` searches at most 1,024 parent-linked journal entries with an active-tip marker and selects exact entry IDs. 70×18 and 90×24 PTYs verified Escape retains drafts, Enter inserts without executing, a worker finished behind the chooser, compact tool help stays readable, and a branched tree selection discarded stale alternate rows. Redirected CLI checked enabled/disabled shell help and `/tree` branch ID selection. Displayed transcript/tree previews strip C0/C1 and bidi control characters.
- **Current unreleased fix / next action:** After `/resume` placed its journal name in the footer, later `/context` and `/hotkeys` notices left that obsolete status visible in public v0.1.12. `Tui.events` now restores the regular footer hints when it renders fresh informational output; a 70×18 source PTY confirmed the status transition for both commands, and complete regressions/build/lint passed. Publish v0.1.13 and prove isolated public update/shipped surface, then continue unchecked tasks without inventing provider usage or claiming parity.
- **Verification:** Full `opam exec -- dune runtest --force`, `opam exec -- dune build @install`, installer syntax and opam lint passed after the footer fix. A 70×18 native PTY rendered `/context` before/after a streamed Ollama turn with 0/2 recorded messages and untracked usage; the `/hotkeys` guide fit ten logical rows. **Publicly upgraded v0.1.12** reopened a private journal, displayed two-message context and keyboard hints, but retained an obsolete journal footer; the corrected source PTY cleared it on both commands. Redirected `/help` omitted terminal shortcuts and `/hotkeys` stated terminal-only support. Earlier local 80×24, 30×10 and 40×12 `NO_COLOR=1` PTYs verified grapheme editing, cancellation, tool details, private journals, narrow layout and no colored SGR.
- **Limits:** Remaining unchecked TASKS.md items include provider-specific semantics and model usage, automatic context budgeting, thinking/diff blocks, full session metadata/lineage, LSP/DAP, subagents, extensibility and remote services. No live vendor credentials, proprietary services, mobile-device builds, Windows/musl binaries or independent update signature were verified. Source/opam binaries cannot self-update; use their package manager.
