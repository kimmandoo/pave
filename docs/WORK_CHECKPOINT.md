# Work checkpoint

- **Date:** 2026-09-26
- **Active task:** R1 model identity and discovery foundation is complete. The user requested stopping at R1; R2 has not started.
- **Current slice:** Added canonical `(provider, account, route, exact upstream ID)` identities and CLI selectors; separated ID and capability provenance; added strict duplicate-listing rejection, typed optional capabilities, bounded cancellable account/route snapshots and fresh-on-open discovery. Migrated CLI, journal, setup/settings and `/model` callers. The picker exposes every provider status page with Tab. The 83-identity inventory records grant/listing evidence and the official Antigravity third-party-use prohibition.
- **Verification:** `opam exec -- dune runtest --force`, `opam exec -- dune build @install` and `opam lint pave.opam` passed after the final code changes.
- **Runtime smokes:** `--models --model 'ollama@chat/org/model/with/slashes'` returned the exact canonical selector from local `/api/tags`; the same selector completed a real local Ollama `/api/chat` request and returned `fixture reply`, with the exact upstream ID in the request. A real 150×34 TUI PTY opened `/model`, showed the exact ID and provider-listing provenance, canceled a delayed listing on Escape in 0.05s, reopened to a fresh listing, advanced to provider-status page 2 with Tab, and exited cleanly. A loopback-only curl guard denied 39 remote listing attempts; none were forwarded.
- **Changed files — docs/config/UI:** `CHANGELOG.md`, `README.md`, `TASKS.md`, `docs/DESIGN_RULES.md`, `docs/PROVIDER_MODEL_DISCOVERY_INVENTORY.md`, `docs/TROUBLESHOOTING.md`, `docs/WORK_CHECKPOINT.md`; `bin/main.ml`, `bin/ui/model_picker.ml`, `bin/ui/settings_view.ml`, `bin/ui/setup_view.ml`, `bin/ui/tui.ml`, `lib/config/settings.ml`, `lib/ui/interaction.ml`.
- **Changed files — provider/session:** `lib/provider/local_compat.ml`, `lib/provider/model_catalog.ml`, `lib/provider/model_discovery.ml`, `lib/provider/model_discovery_coordinator.ml`, `lib/provider/model_identity.ml`, `lib/provider/provider.ml`, `lib/provider/sakana_api.ml`, `lib/provider/tool_gateways.ml`, `lib/provider/transports/bedrock_mantle.ml`, `lib/provider/transports/charm_hyper_api.ml`, `lib/provider/transports/commandcode_api.ml`, `lib/provider/transports/coreweave_api.ml`, `lib/provider/transports/devin_api.ml`, `lib/provider/transports/kilo_api.ml`, `lib/provider/transports/meta_api.ml`, `lib/provider/transports/novita_api.ml`, `lib/provider/transports/nvidia_api.ml`, `lib/provider/transports/ollama_cloud.ml`, `lib/provider/transports/opencode_go_api.ml`, `lib/provider/transports/opencode_zen_api.ml`, `lib/provider/transports/qianfan_api.ml`, `lib/provider/transports/siliconflow_api.ml`, `lib/provider/transports/singularity_dev_api.ml`, `lib/provider/transports/stepfun_api.ml`, `lib/provider/transports/synthetic_api.ml`, `lib/provider/transports/vercel_ai_gateway_api.ml`, `lib/provider/transports/wafer_api.ml`, `lib/provider/transports/xai_api.ml`, `lib/provider/transports/xiaomi_api.ml`, `lib/provider/transports/yolo_auto_api.ml`, `lib/provider/transports/zenmux_api.ml`, `lib/session/session.ml`, `lib/session/session_tree.ml`.
- **Changed files — tests:** `test/dune`, `test/config/test_settings.ml`, `test/provider/test_local_compat.ml`, `test/provider/test_model_discovery.ml`, `test/provider/test_model_discovery_coordinator.ml`, `test/provider/test_sakana_api.ml`, `test/provider/test_tool_gateways.ml`, `test/provider/transports/test_bedrock_mantle.ml`, `test/provider/transports/test_charm_hyper_api.ml`, `test/provider/transports/test_commandcode_api.ml`, `test/provider/transports/test_coreweave_api.ml`, `test/provider/transports/test_devin_api.ml`, `test/provider/transports/test_kilo_api.ml`, `test/provider/transports/test_meta_api.ml`, `test/provider/transports/test_novita_api.ml`, `test/provider/transports/test_nvidia_api.ml`, `test/provider/transports/test_ollama_cloud.ml`, `test/provider/transports/test_opencode_go_api.ml`, `test/provider/transports/test_opencode_zen_api.ml`, `test/provider/transports/test_qianfan_api.ml`, `test/provider/transports/test_siliconflow_api.ml`, `test/provider/transports/test_singularity_dev_api.ml`, `test/provider/transports/test_stepfun_api.ml`, `test/provider/transports/test_synthetic_api.ml`, `test/provider/transports/test_vercel_ai_gateway_api.ml`, `test/provider/transports/test_wafer_api.ml`, `test/provider/transports/test_xai_api.ml`, `test/provider/transports/test_xiaomi_api.ml`, `test/provider/transports/test_yolo_auto_api.ml`, `test/provider/transports/test_zenmux_api.ml`, `test/session/test_session_journal.ml`, `test/session/test_session_store.ml`, `test/ui/test_chooser_dynamic.ml`, `test/ui/test_interaction.ml`.
- **Next action:** No further R1 work. Wait for explicit user direction before starting R2. The separate v0.1.42 release CI/tag/publication remains pending.
- **Blockers:** None for R1. R2 is intentionally out of scope; v0.1.42 release workflow has not been run.

- **Shipped v0.1.39 and local 15-identity follow-on:** Published v0.1.39 passed full suites and four-platform Linux/macOS arm64/x86-64 release packaging/extracted executable smoke; a checksum-verified public v0.1.38 binary updated with a saved private OpenRouter credential still resolving authenticated discovery and private modes/ownership/override safeguards intact. That release contains 45/83 provider descriptors. Local unshipped work first added 15 independently pinned Kilo/Alibaba Coding Plan/SingularityAPI/OpenCode/Charm Hyper/Fire Pass/Yolo Auto/Xiaomi Token Plan/MiniMax Coding Plan/Meta identities for 60/83; all 15 passed native CLI fake-HTTPS tool-result turns around a real workspace file, not live vendor entitlement. Meta and OpenCode Zen preserve signed stateless Responses state. Later additions and the user's TUI priority are recorded below.
- **User-prioritized through-Devin and TUI work:** Local Vercel, Cloudflare configured gateway, Command Code Studio explicit Chat/Messages/Responses, GitLab Duo Direct Access and Devin protobuf/Connect raised exercised sibling inventory to 65/83; signed route/model-bound tool replay and bounded native gzip Connect have isolated two-turn fixtures, and a native fake-HTTPS Devin CLI discovered account models and transmitted a real workspace `read_file` result across two compressed protobuf turns. No live account entitlement or provider OAuth proof. Cursor HTTP/2 duplex and GitLab Duo Agent WebSocket remain unsupported, Antigravity third-party login is prohibited by official terms, and xAI subscription OAuth has no documented reusable client registration. A reproduced `/model` chooser bug selected a longer discovered ID when an exact custom prefix was typed; regression now passes. Native 90×20 and 52×14 PTYs rendered `Use: openai/gpt-4` and selected that exact ID instead of `gpt-4.1-long`; at compact widths the live status occupies its own row while Enter/Esc remain visible.
- **Explicit route TUI/session fix:** `/model commandcode@messages/MODEL` now selects and displays the exact API route; typing another model ID retains it. Setup/settings choose and persist `default_api` for explicit-route providers. Model journal markers include an optional API (old journals still load); `/resume` and branch selection recover the selected route independently of configured defaults. An isolated real 90×20 PTY exercised model switching and journal restart with no vendor traffic; focused settings/interaction/journal tests passed.
- **Native signed-state recovery:** Interactive agent recreation now retains model/route-matching Meta, Devin, Command Code, GitLab Duo and OpenCode Zen provider state. Zen's new state records model provenance; legacy unbound opaque output is discarded rather than guessed safe. Interaction regressions verify matching preservation and cross-route/model rejection; Zen's native pinned fake-HTTPS two-turn reasoning/tool fixture passed.
- **Next discovery slice verified locally:** DeepSeek/Groq/Mistral bearer-key and Anthropic API-key pinned listings use the existing cancellable setup/settings/`/model` picker; Anthropic pages bound cursor depth and fail closed if incomplete. An isolated 70×18 native DeepSeek PTY selected previously unknown `future-model-902` with Down+Enter. Full test suite, install build and opam lint passed. Anthropic OAuth account listing and live vendor entitlement remain unverified; remaining TASKS.md items stay open.
- **Expanded sibling provider parity (ongoing):** Sibling KDL rules declare 83 providers. Pave now has 32 exercised inference descriptors and 51 unsupported providers. Abliteration Responses and GMI Cloud Chat each completed pinned fake-HTTPS native CLI two-turn `read_file` rounds; GMI account listing does not establish per-model tool support. Google Vertex Gemini SSE with derived validated project/location Google host and ADC/explicit token preserved signed function calls across a native two-request CLI tool-result fixture; account listing and managed OAuth remain missing. AWS Bedrock native buffered Converse signed two requests around a real workspace file plus a regional `ListFoundationModels` GET; the listing does not prove Invoke permission, and SSO/roles, binary eventstreams and inference profiles remain absent. Moonshot global Chat and pinned `/v1/models` completed native CLI authenticated discovery plus two requests around a real `read_file` result. Ollama Cloud native Chat and `/api/tags` passed authenticated two-turn fake HTTPS tool replay, separate from local Ollama. AWS Bedrock Mantle uses validated region-bound bearer Responses and bounded account-visible model listing; listed IDs are not guaranteed Responses/tool-capable and no live credentials were used.
- **Additional verified provider slice:** xAI `/v1/language-models` and native Chat preserve actual `reasoning_content` in provider-state on a tool-result replay. NVIDIA hosted NIM `/v1/models` and buffered Chat normalize assistant null content and omit unsupported model-dependent `tool_choice`; its listing does not prove per-model tool capability. Both authenticated fake-HTTPS native CLI scenarios read a real workspace file via two model requests; neither used live vendor credentials. A corrected 90×18 Moonshot `/model` PTY confirmed the slash suggestion with two Enter presses and then displayed `[verified] moonshot/future-moon-model-902` from a pinned account listing, distinct from the initial header model. That slice had 34/83 local providers, with 49 sibling providers and full TASKS.md parity still open.
- **Novita/SiliconFlow + transport security:** Novita and SiliconFlow global passed pinned bearer model-list and actual native CLI two-request `read_file` turns. Novita preserved interleaved reasoning details, SiliconFlow preserved optional reasoning text without inventing per-model thinking switches; no live vendor entitlement tested. A security review found generic API-key completion could forward a bearer to caller-selected remote plaintext HTTP; source now rejects non-local HTTP before either buffered or streamed requests, retaining validated local engines/loopback. Regression covers remote vendor host and deceptive loopback userinfo authority; local provider HTTP fixtures still pass. Current local provider count: 36/83, 47 missing.
- **SiliconFlow China region:** Separate `siliconflow-cn` provider uses `SILICONFLOW_CN_API_KEY` with pinned `.cn` `/v1/models?type=text&sub_type=chat` and `/v1/chat/completions`; exact-host guards reject global-vs-China credential crossing before network. A native CLI fake HTTPS model listing and two-request real workspace `read_file` roundtrip passed; no model IDs or capability inference are bundled. Local coverage: 37 of 83, 46 remaining.
- **StepFun/CoreWeave:** International StepFun `.ai` (not `.com`) credentialed account listing has no modality flags and includes audio-only IDs. CLI labels them Chat/tool unverified; a real 90×18 TUI `/model` displayed `unclassified IDs` and no fake verified audio suggestion. Provider-specific buffered Chat replayed actual `stop` with complete tool calls in two native fake-HTTPS CLI requests around a real workspace file. CoreWeave Serverless used the documented W&B Inference host, `WANDB_API_KEY` fallback and authenticated account listing with real native two-request workspace read. Neither proves live entitlement or every listed ID's tools. Local coverage: 39/83, 44 missing; TASKS.md remains open.
- **Shipped v0.1.37 provider increment and saved-login continuity:** Four-platform release CI passed suites and packaged executable smokes. An isolated public v0.1.36 installation updated to v0.1.37, kept its saved private OpenRouter credential resolving the account model listing and enforced private file modes, update ownership and ignored inherited overrides. The published binary contains 34/83 provider descriptors; Novita/SiliconFlow and the remote-HTTP guard were developed subsequently.
- **Shipped v0.1.38 and local follow-on:** Four native macOS/Linux arm64/x86-64 release jobs passed full OCaml tests and extracted-archive executable smokes. An isolated public v0.1.37 binary self-updated to checksum-verified v0.1.38 while a private saved OpenRouter credential still resolved the pinned account model listing; private file modes, foreign-binary ownership and update override guards held. That tag ships 39/83 sibling providers and the remote plaintext bearer guard. Locally, Synthetic, Z.AI standard Chat, ZenMux, Wafer Serverless, Qianfan V2 and Xiaomi MiMo pay-as-you-go reached 45/83, each with fake HTTPS native CLI two-request real `read_file` tool results. Five lists are account-authenticated; Z.AI lacks documented model listing. Synthetic/ZenMux/Wafer/Xiaomi listings remain unclassified, Qianfan only suggests `type=chat`, and vendor live entitlements remain untested. Z.AI Coding Plan Anthropic and Xiaomi Token Plan routes are not implemented.
- **Shipped v0.1.36 provider increment and login continuity:** Four-platform release CI passed all native suites, packaging and archive executable smokes. An isolated public v0.1.35 binary upgraded to v0.1.36 while its private saved OpenRouter key still resolved a pinned account model listing; file permissions, inherited override isolation and foreign-binary self-update rejection held. The shipped release contains 29 provider descriptors; local Moonshot, Ollama Cloud and Bedrock Mantle changes followed afterward and are not yet in that release.
- **Shipped v0.1.35 gateway/Sakana increment:** Four-platform release CI passed all native tests and archive smokes. An isolated public v0.1.34 installation upgraded to public v0.1.35 while its saved private OpenRouter credential still resolved the pinned listing before and after; private file modes and updater ownership checks held. The shipped native binary now contains 25 functional provider descriptors, including AIML API/ai& capability-filtered Chat and Sakana account-scoped Responses; 58 of 83 sibling providers remain missing. Sakana `FUGU_API_KEY` alternate env and two-request actual workspace tool result were exercised locally, not against the vendor.
- **Shipped v0.1.33 login continuity:** Public v0.1.30→v0.1.31 update first reproduced GitHub's stale `/latest/download` redirect silently reinstalling v0.1.30 while its releases API reported v0.1.31. Since v0.1.32, the embedded updater pins the validated API tag for both archive and checksum; v0.1.33 also refuses downgrade when latest metadata lags. An isolated public v0.1.32 binary updated to v0.1.33 while a saved fake OpenRouter OAuth-exchanged key resolved authenticated pinned `/models` before and after, `update --check` reported current, credential directory/file/lock retained 0700/0600/0600 modes, and the credential stayed outside the install destination. Inherited version/destination overrides were ignored; an unowned copied binary refused self-update. v0.1.33 release CI passed all four packaged archive smokes, and its public installed macOS arm64 70×18 PTY saved a dynamically discovered keyless LM Studio model. No live vendor auth was used.
- **Shipped v0.1.34 provider increment and saved-login continuity:** Four-platform release CI passed tests, native packaging and archive smokes. An isolated public native v0.1.33 installation updated to v0.1.34 with its saved OpenRouter credential still resolving the pinned account listing before and after; private credential permissions persisted, inherited updater overrides were ignored and a copied unowned binary could not update. That release contained Hugging Face/NanoGPT authenticated Chat plus strict public-cloud Azure Responses, for 22 of 83 sibling providers. AIML API, ai& and Sakana were shipped subsequently in v0.1.35; no live vendor credentials were used.
- **Delivered TUI slice:** `Interaction.commands` is the single typed source for slash parsing, `/help` descriptions and `/` + Tab prefix-filtered chooser. `/tools [NAME]` uses the exact shell-policy-filtered provider schemas; `/tree` searches at most 1,024 parent-linked journal entries with an active-tip marker and selects exact entry IDs. 70×18 and 90×24 PTYs verified Escape retains drafts, Enter inserts without executing, a worker finished behind the chooser, compact tool help stays readable, and a branched tree selection discarded stale alternate rows. Redirected CLI checked enabled/disabled shell help and `/tree` branch ID selection. Displayed transcript/tree previews strip C0/C1 and bidi control characters.
- **Delivered TUI slice / next action:** `/context` shows actual selected model/route, branch tip and durable/retained conversation counts. Native Ollama, OpenAI Responses, Codex subscription Responses, cache-aware Anthropic Messages, thoughts-inclusive Google Gemini and usage-reporting Chat Completions valid buffered/final-stream input/output counts persist as private branch-local journal markers; `/usage` groups real counts by provider/model for the selected ancestry, while ephemeral conversations show a combined total without invented model provenance. Missing usage, context limits and cost remain explicitly untracked. `/hotkeys` shares terminal-owned shortcuts with `/help`; informational notices clear stale footer alerts. Continue unchecked TASKS.md provider semantics, context budgets and session lifecycle while retaining TUI-first architecture; do not mark remaining inventory complete.
- **Verification:** Full strict tests/install build, installer syntax and opam lint passed for v0.1.13 and the v0.1.14 usage integration; journal fork-with-usage regression passed. An isolated 70×18 native PTY streamed Ollama counts (18 input/7 output), a buffered turn persisted 36/14 combined, `/resume` reopened those totals, `/branch` before the first usage marker reported untracked and after the marker restored 18/7; an ephemeral turn also displayed 18/7. Two streamed turns plus manual `/compact` retained three real completion counts (54/21). Another 70×18 PTY verified `/usage` empty and populated states and model-specific 36/14 grouping, and redirected `/help` listed the new command. Fixtures used only task-owned state directories and local HTTP. **Publicly upgraded v0.1.13** previously verified journal context, hotkeys and footer restoration. Earlier 80×24, 30×10 and 40×12 `NO_COLOR=1` PTYs verified grapheme editing, cancellation, tool details, private journals, narrow layout and no colored SGR.
- **Narrow usage surface:** Public v0.1.15 `/usage` rendered Ollama model, 18 input/7 output and separate Ollama-only, other-provider and limit/cost disclaimers as complete lines in a real 30×10 `NO_COLOR=1` terminal grid. Only SGR reset codes appeared; no color codes or errors.
- **Shipped usage proof:** The public v0.1.14 native 70×18 PTY streamed a local Ollama response, displayed 18 input/7 output in `/context`, listed the usage journal marker in `/entries`, and reopened the same private session with two messages and the persisted reported totals.
- **Shipped narrow usage proof:** The public v0.1.15 native 30×10 `NO_COLOR` PTY displayed actual Ollama usage (18 input/7 output) with model and untracked-provider/limit/cost lines unbroken; no colored SGR or errors.
- **Guarded retry:** `/retry` joins the typed command catalog and preserves saved prior answers on an abandoned branch; ephemeral retry discards the prior visible answer but retains both real billed token counts. A local HTTP 70×18 PTY observed two distinct saved answers, only two current messages and one selected 18/7 usage record; its final decoded screen showed the new answer and no obsolete answer. `/branch ID` recovered the prior answer without another model call. Another PTY observed 36/14 cumulative ephemeral counts. A real `read_file` tool turn refused retry with no third provider call. Journal tests cover exact parent branch selection, tool rejection, changed-model rejection and root-only sessions.
- **Shipped retry proof:** The public v0.1.16 70×18 PTY reissued a local Ollama prompt exactly once on `/retry`; the final decoded screen showed the second answer, not the discarded first, while `/context` showed 18/7 on the selected branch. No errors or user-home installation writes.
- **OpenAI Responses usage:** A local HTTP 70×18 native TUI streamed `gpt-6-sol` through Responses and displayed 34 input/11 output in `/context` and its saved `/usage` model row. A separate buffered request appended a second private usage marker; restarting the session restored 68 input/22 output. Fake local credentials only; no live vendor entitlement was used.
- **Mixed reported providers:** Separate local buffered OpenAI Responses (34/11) and Ollama (18/7) turns in one private journal showed distinct `openai/gpt-6-sol` and `ollama/smoke` `/usage` rows and a 52-input/18-output `/context` branch total in a 70×18 PTY; neither turn exposed a live vendor credential.
- **Anthropic cache-aware usage:** A local HTTP 70×18 native TUI streamed an Anthropic Messages turn with 2 uncached + 3 cache-created + 4 cache-read input tokens and 7 output; `/context` and `/usage` showed the real 9/7. A buffered request and session restart restored 18 input/14 output. No live vendor credential was used.
- **Narrow three-route usage:** A real 30×10 `NO_COLOR=1` Anthropic PTY showed `anthropic/claude-fixture`, cache-inclusive 9 input/7 output and complete compact supported/unsupported route and cost disclaimers as five unbroken rows. No colored SGR or errors.
- **Gemini usage:** Direct Gemini buffered and SSE routes now record `promptTokenCount` including cache hits and `candidatesTokenCount + thoughtsTokenCount` after successful completions; a terminal usage-only frame after STOP is accepted without allowing more candidate output. A local HTTP 70×18 PTY streamed 12/8 in the selected Google model row; a prior buffered request plus streamed request persisted a combined 24/16. Another real 30×10 `NO_COLOR=1` PTY showed `google/gemini-2.5-flash`, 12 input/8 output and three intact provenance/unknown-limit lines; only SGR reset was present. No live vendor entitlement used; other routes, limits and cost remain untracked.
- **Chat Completions usage:** Buffered and streamed OpenAI-compatible responses can record complete `prompt_tokens`/`completion_tokens` only after a valid assistant reply; official OpenAI Chat streaming proactively requests the documented final usage-only frame, whereas compatible/Copilot routes receive no extra option and remain untracked unless they report actual counts. Local DeepSeek-compatible HTTP buffered plus SSE turns produced a 70×18 TUI `deepseek/deepseek-chat` row totaling 38 input/14 output. No vendor entitlement used.
- **Shipped v0.1.17 usage proof:** Public `v0.1.16` installed in a task-owned destination, upgraded to `v0.1.17` over HTTPS, then reported current. The upgraded 70×18 native PTY streamed a local Anthropic completion, showed `Shipped Claude OK` and `anthropic/claude-sonnet-4-5` with cache-inclusive 9 input/7 output in saved `/usage`, and exited cleanly. No live credentials or user-home writes.
- **Incomplete Chat SSE fix:** Reproduced an actual partial DeepSeek-compatible SSE text followed by `[DONE]` without any finish reason: before, headless CLI exited 0 with `Unfinished reply`. After strict finish validation, headless exits 1, and a real 70×18 TUI clears the provisional answer, shows the completion error and `/usage` reports no saved tokens. Contradictory `stop` plus tool calls is also rejected before tool execution; regression covers both.
- **Readable TUI errors:** Known provider/OAuth/tool/storage failures share one CLI formatter for worker, command and startup paths; unknown exceptions keep diagnostic constructors. A local truncated SSE 70×18 TUI rolled back the provisional assistant reply and displayed `Error: invalid completion response: missing finish_reason` on one row. The redirected CLI returned 1 with the same readable stderr.
- **TUI live usage badge:** The idle header caches authoritative selected-branch or cumulative ephemeral reported input/output tokens, recalculated only on turn completion and journal navigation (not editor keystrokes). A 30×10 `NO_COLOR` Gemini PTY showed `12 in/8 out`, removed the badge on a pre-usage branch, restored it after returning to the answer branch and reopening the journal; ephemeral `/retry` showed 24 in/16 out despite replacing the old visible answer. A separate slow local response kept `Working` visible without stale counts during the next request, then showed 24 in/16 out on completion. Unknown usage has no badge.
- **Codex metered SSE:** The account-scoped provider reports nonnegative `usage.input_tokens`/`output_tokens` only after its own validated completed SSE envelope; missing/negative fields remain unknown. A task-owned fake-curl executable accepted only the pinned ChatGPT Codex endpoint and account header, preventing any vendor network; an isolated fake OAuth credential and saved 70×18 native TUI completed two Codex turns and displayed `openai-codex/gpt-5.1-codex`, 42 input/18 output in both `/usage` and the idle header. No live entitlement, account spending or pricing was asserted.
- **Shipped v0.1.18 Gemini proof:** A task-owned public `v0.1.17` installation upgraded to `v0.1.18` over HTTPS and reported current. Its shipped 30×10 `NO_COLOR=1` native TUI streamed a local Gemini response with a separate usage-only final frame; `/usage` displayed the full `google/gemini-2.5-flash` model, 12 input/8 output and complete provenance/unknown-cost rows. SGR contained only reset, and no user-home installation was touched.
- **Typed tool status:** `Agent.phase` sends model/tool execution stages through `Turn_runner`'s ordered pipe; the UI thread alone updates the header, while headless turns allocate no phase callback. A 70×18 real TUI local Chat tool turn requested a shell command, displayed its exact approval and waited for `y`; the header showed `Tool: run_command` during the 0.8s approved command, `Working` during a separately delayed follow-up model request, then an idle 15-input/7-output badge alongside the completed tool block and model answer. No tool command ran without explicit approval.
- **Shipped v0.1.19 upgrade and status proof:** Four native macOS/Linux arm64/x86-64 archives and their SHA-256 manifest were published after the release workflow passed. A task-owned public v0.1.18 installation showed v0.1.19 available, updated over HTTPS to its original private destination, then reported current. Its shipped 30×10 `NO_COLOR=1` TUI streamed a local Gemini completion and a separate trailing usage-only event, showed 12 in/8 out in the idle header and reported totals in `/usage`, and emitted no non-reset SGR. No user-home installation or live provider was used.
- **Shipped v0.1.20 typed TUI proof:** Release CI passed for macOS/Linux arm64/x86-64 and published all four native archives plus checksums. An isolated public v0.1.19 installation upgraded itself over HTTPS, then reported v0.1.20 current. Its shipped 70×18 `NO_COLOR` PTY showed the exact shell approval requiring `y`, `Tool: run_command` during an approved 0.8-second command, `Working` during the delayed follow-up model request, then an idle 15-input/7-output badge with the tool result and final answer visible. The two Chat requests reached only a task-owned local server, and no non-reset SGR or user-home installation was involved.
- **Active elapsed TUI:** `Terminal_input` emits a timed wake only while an actual turn is active, and `Tui` retains one turn start across typed model/tool phases, the chooser and the shell approval. A 30×10 `NO_COLOR` native PTY streamed a deliberately delayed local Chat answer: header `Working · 1s`, then `Working · 2s`, then idle 6-in/2-out reported usage; 0.7 seconds of idle waiting produced zero terminal bytes. A 70×18 PTY showed `Tool: run_command · 1s` while waiting for a visible `y` approval, `Working · 2s` during the subsequent model request, and the prior 15-in/7-out idle usage on completion.
- **Shipped v0.1.21 elapsed status proof:** Release CI passed on four native macOS/Linux arm64/x86-64 targets and published checksums; a task-owned public v0.1.20 binary upgraded over HTTPS to v0.1.21 and reported current. Its shipped 30×10 `NO_COLOR` PTY rendered `Working · 1s`, `Working · 2s`, the completed answer and real 6-in/2-out idle usage against a delayed local model server. It emitted zero bytes during a subsequent 0.6-second idle interval and no non-reset color SGR; no user-home installation was touched.
- **First-run setup and live slash hints:** The settled first screen in an isolated 70×18 fresh PTY was `SETUP · Welcome to Pave` before any status marker existed; Escape persisted `skipped`. In a separate 30×10 `NO_COLOR` PTY, provider `ollama` and custom `ollama/llama3.2` model confirmation wrote user defaults and a `complete` status, switched the active header immediately, and a restart reopened directly on that model without setup. The `/` inline list rendered actual command names/usage/descriptions; `/re` showed `/resume` and `/retry`; Down+Enter inserted `/retry` without side effects, a second Enter ran it, and Escape retained a slash draft. At 30×10 Tab inserted the selected command and no non-reset color SGR appeared. A local headless Chat `--prompt` returned only its answer with no setup marker.
- **Setup scene layout:** The new chooser `intro` is transient, sanitized and only shown when there is room (at least 52 columns and nine body rows); it disappears on filter input so selections remain legible. A 70×18 `NO_COLOR` PTY showed `YOUR WORKSPACE, YOUR MODEL` and `01 / 03 · PROVIDER` explanations with eight visible provider rows; a 30×10 PTY kept the compact two-choice welcome. Resizing that live picker from 30×10 to 70×18 displayed the guidance; filtering `ollama` left the correct provider selected and visible. No non-reset color SGR was emitted.
- **Shipped v0.1.22 install UX proof:** Four macOS/Linux arm64/x86-64 native archives and checksums were published after CI passed. A task-owned public v0.1.21 binary updated itself over HTTPS to v0.1.22 and reported current; its shipped 30×10 `NO_COLOR` PTY opened first-run `SETUP · Welcome to Pave`, saved `ollama/llama3.2` under the isolated XDG user config with a `complete` marker, showed both `/resume` and `/retry` while typing `/re`, inserted `/retry` without executing until a second Enter, and emitted no non-reset SGR. No user-home installation or live provider was used.
- **Shipped v0.1.23 guided setup proof:** Four native macOS/Linux arm64/x86-64 archives and checksums were published after release CI passed. A task-owned public v0.1.22 binary upgraded over HTTPS to v0.1.23 and reported current. Its shipped 70×18 `NO_COLOR` TUI displayed the first-run `YOUR WORKSPACE, YOUR MODEL` welcome, `01 / 03 · PROVIDER` instructions, `03 / 03 · MODEL` guidance and `READY · REVIEW` stage. Selecting `ollama/llama3.2` wrote an isolated user `complete` setup marker. No non-reset color SGR, live provider or user-home modification occurred.
- **Shared live first-run model picker:** Extracted the existing cancellable listing worker into `Model_picker`, used by both `/model` and first-run setup; credential resolution remains pinned to registered provider listing endpoints. In an isolated 70×18 native PTY, a task-owned loopback server on Ollama's documented `/api/tags` returned two models. The setup chooser displayed `[verified] ollama/llama3.2:latest` first, ahead of untagged Back/Skip actions, Enter selected and saved that exact ID, and a separate in-session `/model` showed the same verified result. A focused chooser regression preserved a user-selected Back action when later verified results changed. No live external service was called.
- **Shipped v0.1.24 live setup model proof:** Release CI passed four native macOS/Linux arm64/x86-64 targets and published their archives plus SHA-256 manifest. A task-owned public v0.1.23 installation reported v0.1.24 available and upgraded over HTTPS. Its shipped 70×18 `NO_COLOR` PTY started on the first-run setup; a task-owned loopback `/api/tags` response supplied two Ollama models, and the chooser displayed `[verified] ollama/llama3.2:latest` and `2 live routable models`. Enter advanced through review into a session showing that model, with `settings.json` and `setup.json` created only inside the isolated XDG root. No live vendor provider or user-home installation was used.
- **Shipped v0.1.25 distribution smoke:** Four native macOS/Linux arm64/x86-64 release jobs passed a new extracted-archive executable test (`--help`, `--providers`) before upload and published the archives plus checksum manifest. A task-owned local arm64 archive passed the same extracted-binary scenario. Signing/notarization is optional distribution hardening for macOS binaries, not an execution prerequisite or blocker for terminal features.
- **Shipped v0.1.26 arrow-first setup:** Four native release targets built, tested, unpacked and smoke-executed their archived binaries before publication. A public v0.1.26 installer into a task-owned directory launched a fresh 70×18 `NO_COLOR` PTY; a loopback `/api/tags` fixture returned two Ollama model IDs. The setup automatically displayed both as verified models, Down highlighted `ollama/qwen2.5:latest`, Enter opened review, and a second Enter saved `qwen2.5:latest` without typing any model ID. A separate 30×10 local PTY pasted fragmented bracketed delimiters, newline, CJK and Tab, then undid/redid the whole draft without submitting. No live OAuth/provider or user-home installation was used.
- **Shipped v0.1.27 signed-in Codex model list:** Release CI passed the four native archive tests and extracted-executable smoke jobs and published checksums. A task-owned public v0.1.27 installer launched a fresh 70×18 `NO_COLOR` PTY with an isolated saved fake Codex OAuth JWT/account and a fake `curl` that accepts only the fixed ChatGPT `/codex/models?client_version=0.155.1` URL and correct bearer/account headers. The setup's saved-sign-in path loaded two `[verified] openai-codex/...` models, Down+Enter chose the second, and review saved `openai-codex/gpt-6-sol` without typing an ID. Wrong credentials, 401, 302 and malformed listings are covered by model-discovery fixtures; no live OpenAI account, vendor network or user-home installation was used.
- **Limits:** Row116 has explicit-window byte-proxy budgeting, direct OpenAI Responses native compaction and Command Code live `context_length` metadata; other-provider context/tokenizer data, image token costs and native compaction for other routes remain open. Other unchecked `TASKS.md` work includes provider-specific semantics/usage, thinking/diff blocks, LSP/DAP, subagents, extensibility and remote services. No live vendor credentials, proprietary services, mobile-device builds, Windows/musl binaries or independent update signature were verified. Source/opam binaries cannot self-update; use their package manager.
