# Work checkpoint

- **Date:** 2026-09-24
- **Active task:** The latest user prioritizes maintainable source ownership and reference-style in-session `/login` and `/model`. Library, executable UI and tests are grouped by responsibility; browser login and model selection are now usable after entering an otherwise uncredentialed full-screen terminal. Nine provider descriptors and six wire transports have isolated fixture verification; no live vendor account was exercised. The public MIT repository, installer, release artifacts, README and mark remain available.
- **Exact next action:** Continue remaining reference OAuth/provider transports and real model catalog/selector semantics; then complete TUI/agent/tools/extensibility parity and final responsibility review. Preserve native state and login security across route changes, and verify new provider methods through actual transport scenarios before advertising them.
- **Changed files in this wave:** `lib/ui/interaction.ml` owns slash parsing, provider/model routing and opaque Codex-state boundaries; `test/ui/test_interaction.ml` covers selector precedence and state isolation. `bin/cli_auth.ml` exposes a shared sign-in action without exiting in-session, `bin/main.ml` lazily resolves credentials and rebuilds the agent after selection, and `bin/ui/tui.ml` releases/restores normal terminal mode for browser URLs while retaining the transcript. Updated `test/dune`, `README.md`, `CONTRIBUTING.md`, `TASKS.md`, `CHANGELOG.md`, `SECURITY.md`, `docs/DESIGN_RULES.md` and this checkpoint. Earlier structural cutover was committed as `a0f129e`.
- **Verification:** `opam exec -- dune runtest --force` passed 25 suites and `opam exec -- dune build @install` succeeded after slash integration. Native PTY exercised explicit and no-argument `/login` and `/model`, a real loopback callback and fake-HTTPS OpenRouter key exchange, private 0600 storage, two authenticated turns on different model IDs with shared history, terminal restoration and recovery after invalid login. The line CLI reopened that journal, selected OpenAI GPT-5 Responses automatically and verified visible context crossed providers without forwarding the prior key; a separate unsaved two-turn session preserved history across model changes, and `--logout` removed the stored grant. All vendor responses were isolated fixtures; no live vendor account was used. Earlier native installer, CJK PTY and public four-platform `v0.1.2` publishing passed.
- **Limits:** Live vendor credentials, account entitlements and actual mobile device builds remain unverified. Most reference auth policies/transports and many agent/tools/TUI/extensibility features remain incomplete. Earlier mandatory MIT notices remain in `THIRD_PARTY_NOTICES`; rewriting local/remote Git refs does not erase copies held by outside caches.
