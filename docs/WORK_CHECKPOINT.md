# Work checkpoint

- **Date:** 2026-09-25
- **Active task:** Added a native self-update path for installer-owned binaries and compared the sibling reference tree to Pave. `TASKS.md` now groups independently actionable, source-indexed work for catalog/providers, authentication, agent/session, workspace/IDE, TUI, extensions and optional remote/auxiliary systems. Existing provider and login baseline remains ten descriptors, six wire payload formats and four sign-in paths verified only against local fixtures; full parity is **not** complete.
- **Exact next action:** Verify the entire OCaml suite and update scenarios, commit and publish the native updater across all four release targets, then exercise an actual newly installed release's `pave update`. Subsequent feature work should take individual open tasks from the reference-derived backlog, implementing a real consumer scenario before checking them off.
- **Changed files in this wave:** `install.sh` writes a native-install marker after validated assets and before its atomic binary rename. `bin/build/embed_installer.ml` generates OCaml source containing that checked-in shell script; `bin/dune` compiles it into `bin/update.ml`; `bin/main.ml` dispatches `pave update` before normal argument handling. `TASKS.md` contains the reference-derived backlog. `README.md`, `SECURITY.md`, `docs/DESIGN_RULES.md`, `CONTRIBUTING.md` and `CHANGELOG.md` describe use, migration and trust limits.
- **Verification:** Development and release-profile binaries built locally. `opam exec -- dune runtest --force` passed 27 suites, `opam exec -- dune build @install`, `opam lint pave.opam` and `sh -n install.sh` passed. An isolated fake HTTPS release archive installed a native Pave into a custom directory; its executable-invoked update replaced it with a different working binary, ignored a foreign `PAVE_INSTALL_DIR` and stale `PAVE_VERSION`, and left the original executable intact on a checksum mismatch. The installed binary also updated successfully when invoked as `pave update` via `PATH`. Source-built `pave update` refused before network access. CI and public release/update remain to be checked.
- **Limits:** `pave update` reinstalls latest rather than checking versions and is available only after native installs acquire the ownership marker; a source/opam install needs its package manager. Manifest and archive share a release origin and are not independently signed. Live vendor credentials, actual mobile device builds and full reference feature parity remain unverified. Mandatory prior MIT notices remain in `THIRD_PARTY_NOTICES`; remote cache copies cannot be erased by Git history rewriting.
