# Work checkpoint

- **Date:** 2026-09-25
- **Active task:** The user prioritizes a beautiful, robust TUI, a modern model default and the full reference-derived task inventory. `v0.1.8` is published across macOS/Linux arm64/x86-64; public release workflow `36041347416` passed. An isolated previously published `v0.1.7` binary upgraded over actual HTTPS to `v0.1.8` and `pave update --check` reported current. OpenAI defaults to `gpt-6-sol` on Responses; personal Copilot stays guarded to its older supported Chat models. **Full reference parity is not complete.**
- **Delivered TUI vertical slice:** Pure `bin/ui/transcript_view.ml` semantic roles, sanitized Markdown/code, bounded lazy grapheme wrapping, finished/interrupted tool states and `Alt+O` expandable results. `lib/ui/composer.ml` has bounded undo/redo, atomic paste and kill/yank with real lower-case Meta shortcuts. `lib/session/session_store.ml` creates private per-workspace journals; `/new`, `/resume` and `--session` restoration keep navigation and unsent drafts safe. `Tui` alone owns repaint and key dispatch; agent and provider contracts are unchanged.
- **Exact next action:** Continue unchecked TASKS.md work in verified end-to-end vertical slices, especially native session metadata, accurate provider usage/context, approvals and tool/IDE lifecycle. Keep TUI workflows and visual proof primary; do not claim unavailable vendor entitlements or services.
- **Verification:** Full `opam exec -- dune runtest --force`, `opam exec -- dune build @install`, installer shell syntax and opam lint passed. Actual local-Ollama PTYs at 80×24 and resized 30×10 streamed role/code blocks, preserved Unicode drafts through undo/redo, created 0600 workspace journals, confirmed unsaved-discard prompt, and restored saved history from `/resume`; a 40×12 `NO_COLOR=1` PTY displayed journal content without non-reset colored SGR. A cancelled streaming turn retracted provisional text while a queued prompt completed. A real agent `read_file` turn in the PTY showed compact output, expanded all file lines and re-collapsed on `Alt+O`. The **publicly upgraded v0.1.8 binary** rendered a 60×16 PTY and expanded all five file lines before clean `/quit`. MacOS PTY intercepted `Ctrl+O` as VDISCARD; the shipped shortcut is verified `Alt+O`.
- **Limits:** Remaining unchecked TASKS.md items include provider-specific semantics and model usage, automatic context budgeting, thinking/diff blocks, full session metadata/lineage, LSP/DAP, subagents, extensibility and remote services. No live vendor credentials, proprietary services, mobile-device builds, Windows/musl binaries or independent update signature were verified. Source/opam binaries cannot self-update; use their package manager.
