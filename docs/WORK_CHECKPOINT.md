# Work checkpoint

- **Date:** 2026-09-25
- **Active task:** Delivered installer-owned native self-update and a source-indexed inventory with 74 open tasks for useful reference features. The user's subsequent TUI request adds a launch-only ASCII rendering of the existing Pave pixel mark; no upstream branding or scripts were imported. The provider/auth baseline remains ten descriptors, six payload formats and four sign-ins verified only against fixtures; full parity is **not** complete.
- **Exact next action:** Finish the four-architecture `v0.1.5` release and exercise a real installed binary's `pave update`; then commit/publish the ASCII TUI change and verify a release containing it. Future feature work should select individual unchecked tasks from the reference-derived backlog.
- **Changed files in this wave:** `install.sh` adds an install-ownership marker; `bin/build/embed_installer.ml` and `bin/dune` embed the checked-in script; `bin/update.ml` and `bin/main.ml` implement the installed-binary update command. `TASKS.md` lists source-indexed work. `bin/ui/tui.ml` draws the SVG-derived, centered mint/shadow/amber ASCII logo only over an empty interactive transcript, with a compact fallback. `README.md`, `SECURITY.md`, `docs/DESIGN_RULES.md`, `CONTRIBUTING.md` and `CHANGELOG.md` document behavior and limits.
- **Verification:** `opam exec -- dune runtest --force` passed 27 suites, `dune build @install`, the local release-profile build, `opam lint pave.opam` and `sh -n install.sh` passed. An installed native executable updated to a different working binary against fake HTTPS assets, kept its previous binary after a checksum mismatch, ignored inherited destination/version overrides and refused an invalid marker; source-built binaries refused self-update. Both pushed commits passed four-platform public CI. A native 80×24 PTY showed the square-proportioned ASCII mark; an 8×16 PTY fell back to `PAVE`, resizing back restored the mark, `/help` replaced it with transcript content, and `/quit` exited cleanly. Redirected CLI output remained plain. Public release assets and live latest update remain to be checked.
- **Limits:** `pave update` reinstalls latest rather than checking versions and is available only after native installs acquire the ownership marker; a source/opam install needs its package manager. Manifest and archive share a release origin and are not independently signed. Live vendor credentials, actual mobile device builds and full reference feature parity remain unverified. Mandatory prior MIT notices remain in `THIRD_PARTY_NOTICES`; remote cache copies cannot be erased by Git history rewriting.
