# Work checkpoint

- **Date:** 2026-09-25
- **Active task:** The user prioritizes a beautiful, robust TUI, a modern model default and the full reference-derived task inventory. `v0.1.10` is published across macOS/Linux arm64/x86-64; release workflow `36045921680` passed. An isolated public `v0.1.9` installation upgraded over actual HTTPS to `v0.1.10` and `pave update --check` reported current. OpenAI defaults to `gpt-6-sol` on Responses; personal Copilot stays guarded to its older supported Chat models. **Full reference parity is not complete.**
- **Delivered TUI slice:** Pure semantic transcript, sanitized Markdown/code and tool lifecycle blocks, lazy grapheme wrapping, `Alt+O` expansion, bounded atomic undo/paste/kill/yank, private `/new`/`/resume`, and authoritative `/branch`/`/fork` transcript restoration. The public v0.1.9 PTY checked out a saved second turn then the first, proving abandoned rows disappeared after navigation.
- **Delivered TUI slice:** `Interaction.commands` is the single typed source for slash parsing, `/help` descriptions and `/` + Tab prefix-filtered chooser. `/tools [NAME]` uses the exact shell-policy-filtered provider schemas; `/tree` searches at most 1,024 parent-linked journal entries with an active-tip marker and selects exact entry IDs. 70×18 and 90×24 PTYs verified Escape retains drafts, Enter inserts without executing, a worker finished behind the chooser, compact tool help stays readable, and a branched tree selection discarded stale alternate rows. Redirected CLI checked enabled/disabled shell help and `/tree` branch ID selection. Displayed transcript/tree previews strip C0/C1 and bidi control characters.
- **Current unreleased slice / exact next action:** Private `/resume` titles/timestamps and `/entries` previews now use bounded C0/C1/bidi-safe display while preserving the original journal. Provider/model changes are parent-linked metadata separate from messages; explicit CLI overrides win, saved model selection reappears on `/resume`, `--session` and `/branch`, and first-user title previews skip metadata rows. Branch-point markers shown in `/tree` are now actually selectable. Publish this native session cutover as `v0.1.11`, verify isolated real update and shipped PTY; then continue remaining unchecked tasks without claiming model usage/context or other unimplemented metadata.
- **Verification:** Complete `opam exec -- dune runtest --force` and `opam exec -- dune build @install` passed after metadata cutover; shell syntax and opam lint passed before that (unchanged installer/package). A native redirected CLI reopened an ESC/bidi-containing prompt and listed safe `/resume` and `/entries` text. Real 90×24 PTYs changed and saved a model, restored it on fresh startup and via `/resume`, then checked out two ancestor markers and visibly switched status between model IDs. A real local Ollama HTTP turn with explicit provider/model/endpoint flags recorded its chosen metadata before user and assistant entries. Public v0.1.10 in a 90×24 PTY searched the slash palette, listed enabled tools and selected a saved branch; earlier public v0.1.8/9 PTYs exercised tool expansion and branch repaint. Earlier local PTYs at 80×24, 30×10 and 40×12 `NO_COLOR=1` verified grapheme editing, cancellation, tool details, private journals, narrow layout and no colored SGR.
- **Limits:** Remaining unchecked TASKS.md items include provider-specific semantics and model usage, automatic context budgeting, thinking/diff blocks, full session metadata/lineage, LSP/DAP, subagents, extensibility and remote services. No live vendor credentials, proprietary services, mobile-device builds, Windows/musl binaries or independent update signature were verified. Source/opam binaries cannot self-update; use their package manager.
