# Work checkpoint

- **Date:** 2026-09-24
- **Active task:** Complete the reference provider/authentication surface before the remaining agent, terminal, tools and extensibility phases. Nine provider descriptors and six wire transports have isolated CLI fixture verification. Anthropic/Codex OAuth and OpenRouter browser-to-key login work in local fixtures; no live vendor account was exercised. The public MIT repository, one-command installer, release artifacts, README and pixel-art mark are in place. Perform the requested directory/file responsibility restructuring only after product capabilities are finished.
- **Exact next action:** Add remaining authorization-code/device-code policies and matching native inference transports (Google subscription, Copilot and other gateways have distinct contracts), plus Gemini thought signatures and model-specific state. Preserve unsupported-model fail-closed behavior and verify each provider with a real transport scenario before advertising it. Then finish TUI/agent/tools phases and reorganize the directory graph last.
- **Changed files in the latest wave:** `lib/codex_oauth.ml`, `lib/codex_wire.ml`, `lib/codex_stream.ml`, `lib/openrouter_oauth.ml`, `lib/oauth_flow.ml`, `lib/provider_catalog.ml`, `lib/provider.ml`, `lib/protocol.ml`, `lib/session.ml`, `lib/agent.ml`, `bin/main.ml`, `test/dune`, associated OAuth/wire/stream/provider tests, `README.md`, `TASKS.md`, `CHANGELOG.md`, `SECURITY.md`, `docs/DESIGN_RULES.md` and this checkpoint. Earlier TUI, installer, license and public-repository files were committed separately.
- **Verification:** `opam exec -- dune runtest --force` passed 24 suites and `opam exec -- dune build @install` succeeded after the JWT residency and documentation changes. The native CLI handled nine provider descriptors in local fixtures, including two Codex SSE/tool turns with journal reopening and encrypted-reasoning replay, enterprise residency, OpenRouter PKCE-to-key, logout and 0700/0600 storage; all vendor HTTPS responses were fake. Earlier opam source install, anonymous public HTTPS installer, real CJK PTY, local checksum corruption, lint and shell checks passed. Public CI passed four jobs and `v0.1.2` GitHub Release passed all four architecture builds and publishing.
- **Limits:** Live vendor credentials, account entitlements and actual mobile device builds remain unverified. Most reference auth policies/transports and many agent/tools/TUI/extensibility features remain incomplete. Earlier mandatory MIT notices remain in `THIRD_PARTY_NOTICES`; rewriting local/remote Git refs does not erase copies held by outside caches.
