# Work checkpoint

- **Date:** 2026-09-25
- **Active task:** The user requested a substantially better terminal UX, current defaults and completion of the full reference-derived task inventory with maintainable boundaries. `v0.1.6` is published across macOS/Linux arm64/x86-64. In an isolated custom directory, the previously public-installed `v0.1.5` binary upgraded over actual HTTPS to `v0.1.6`; `pave update --check` reported it current, and its PTY showed the pixel-art ASCII logo. OpenAI now defaults to `gpt-6-sol` on Responses; personal Copilot remains guarded to its older supported Chat models. **Full reference parity is not complete.**
- **Exact next action:** Integrate, commit and release the post-`v0.1.6` changes: guarded path-scoped rules on file mutations, safe discovered/explicit prompt customization, cancellable background authenticated `/model` listings, Gemini signed-model prefix handling and model-first terminal status. Continue unchecked TASKS.md items in verified vertical slices (session metadata/resume, provider transport semantics, approvals/tools, IDE lifecycles and optional extensibility) without claiming vendor accounts or remote services work without real entitlements/trust models.
- **Local post-release changes:** `lib/config/{project_context,system_prompt}.ml`, `lib/agent/agent.ml`, `lib/provider/{model_discovery,transports/gemini_wire}.ml`, `bin/{main,ui/tui}.ml`, tests for scoped turns, prompt precedence, dynamic picker, cancellation and Gemini replay, plus README/SECURITY/DESIGN_RULES/TASKS. Prompt safety remains first; scoped writes are withheld and journaled before rule exposure, then retried; picker network work runs off the UI thread and is cancelled on Escape. Four-platform published `v0.1.6` does **not** include these later changes.
- **Verification:** Four-platform `v0.1.6` Release workflow `36034556994` passed, published SHA-256 assets, and a real public binary update/check and PTY logo were observed. Post-tag local `opam exec -- dune runtest --force` and `opam exec -- dune build @install` passed after integration. Actual CLI Responses request contained fixed mobile safety, project `AGENTS.md` and explicit custom/append text (not overridden discovered text) and used model `gpt-6-sol`. PTY with delayed local Ollama listing kept typed filter responsive, then painted `[verified] qwen3:8b` and exited on Escape without sending a model prompt. Model-first status line displayed GPT-6 in a long-root 80-column PTY. Gemini prefixed signed-replay fixture passed.
- **Limits:** Model discovery is only implemented for pinned OpenAI, Gemini, personal Copilot and local Ollama listings, with no durable entitlement cache or Codex listing. The TUI still lacks structured transcript, session search/resume commands, automatic context budgeting and many TASKS.md features. No live vendor credentials, proprietary services, mobile device builds, independent update signature, Windows or musl artifacts were verified. Source/opam binaries cannot self-update; use their package manager.
