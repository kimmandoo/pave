# Work checkpoint

- **Date:** 2026-09-25
- **Active task:** The user prioritizes a beautiful, robust TUI, a modern model default and the full reference-derived task inventory. `v0.1.13` is published across macOS/Linux arm64/x86-64; release workflow `36054157144` passed. An explicitly isolated public `v0.1.12` installation upgraded over actual HTTPS to `v0.1.13` and `pave update --check` reported current. OpenAI defaults to `gpt-6-sol` on Responses; personal Copilot stays guarded to its older supported Chat models. **Full reference parity is not complete.**
- **Delivered TUI slice:** Pure semantic transcript, sanitized Markdown/code and tool lifecycle blocks, lazy grapheme wrapping, `Alt+O` expansion, bounded atomic undo/paste/kill/yank, private `/new`/`/resume`, and authoritative `/branch`/`/fork` transcript restoration. The public v0.1.9 PTY checked out a saved second turn then the first, proving abandoned rows disappeared after navigation.
- **Delivered TUI slice:** `Interaction.commands` is the single typed source for slash parsing, `/help` descriptions and `/` + Tab prefix-filtered chooser. `/tools [NAME]` uses the exact shell-policy-filtered provider schemas; `/tree` searches at most 1,024 parent-linked journal entries with an active-tip marker and selects exact entry IDs. 70×18 and 90×24 PTYs verified Escape retains drafts, Enter inserts without executing, a worker finished behind the chooser, compact tool help stays readable, and a branched tree selection discarded stale alternate rows. Redirected CLI checked enabled/disabled shell help and `/tree` branch ID selection. Displayed transcript/tree previews strip C0/C1 and bidi control characters.
- **Delivered TUI slice / next action:** `/context` shows actual selected model/route, branch tip and durable/retained conversation counts. Native Ollama's valid buffered/final-stream prompt and completion counts persist in private per-completion branch-local journal markers; `/usage` groups those real counts by provider/model for the selected ancestry, while ephemeral conversations show a combined total. Unsupported providers, context limits and cost remain explicitly untracked. `/hotkeys` shares terminal-owned shortcuts with `/help`; informational notices clear stale footer alerts. Continue unchecked TASKS.md provider semantics, context budgets and session lifecycle while retaining TUI-first architecture; do not mark remaining inventory complete.
- **Verification:** Full strict tests/install build, installer syntax and opam lint passed for v0.1.13 and the v0.1.14 usage integration; journal fork-with-usage regression passed. An isolated 70×18 native PTY streamed Ollama counts (18 input/7 output), a buffered turn persisted 36/14 combined, `/resume` reopened those totals, `/branch` before the first usage marker reported untracked and after the marker restored 18/7; an ephemeral turn also displayed 18/7. Two streamed turns plus manual `/compact` retained three real completion counts (54/21). Another 70×18 PTY verified `/usage` empty and populated states and model-specific 36/14 grouping, and redirected `/help` listed the new command. Fixtures used only task-owned state directories and local HTTP. **Publicly upgraded v0.1.13** previously verified journal context, hotkeys and footer restoration. Earlier 80×24, 30×10 and 40×12 `NO_COLOR=1` PTYs verified grapheme editing, cancellation, tool details, private journals, narrow layout and no colored SGR.
- **Narrow usage surface:** The current `/usage` renders model, 18 input/7 output and separate Ollama-only, other-provider and limit/cost disclaimers as complete lines in a real 30×10 `NO_COLOR=1` terminal grid. Only SGR reset codes appeared; no color codes or errors.
- **Limits:** Remaining unchecked TASKS.md items include provider-specific semantics and model usage, automatic context budgeting, thinking/diff blocks, full session metadata/lineage, LSP/DAP, subagents, extensibility and remote services. No live vendor credentials, proprietary services, mobile-device builds, Windows/musl binaries or independent update signature were verified. Source/opam binaries cannot self-update; use their package manager.
